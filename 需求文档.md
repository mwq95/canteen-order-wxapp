# 食堂订餐小程序需求文档

## 1. 项目背景

为单位新建食堂设计一个微信小程序，用于统计用餐需求，方便员工订餐和食堂管理。

## 2. 功能需求

### 2.1 用户角色

| 角色 | 说明 | 权限范围 |
|------|------|----------|
| staff（普通工作人员） | 已验证的普通员工 | 订餐、查看订单、评价、查看个人统计 |
| kitchen（厨房工作人员） | 食堂厨房人员 | staff权限 + 菜单管理、订餐名单、数据统计、订餐设置 |
| admin（管理员） | 系统管理员 | kitchen权限 + 用户管理 |

### 2.2 核心功能

#### 2.2.1 身份验证
- 微信手机号授权验证
- 手动输入手机号验证
- 首次使用需验证身份，后续自动识别

#### 2.2.2 菜单管理（kitchen/admin）
- 按日期设置每日菜单
- 支持早中晚三餐
- 每个餐次可添加多个菜品

#### 2.2.3 订餐功能
- 查看当日/次日菜单
- 选择菜品进行订餐
- 支持修改已订菜品
- 订单以"顿"为单位（每顿独立订单）

#### 2.2.4 订单管理
- 查看历史订单
- 截止时间前可取消预约
- 过开饭时间自动标记为已完成

#### 2.2.5 订餐名单（kitchen/admin）
- 按日期查看各餐订餐情况
- 显示每道菜的份数统计
- 支持查看订单明细

#### 2.2.6 数据统计（kitchen/admin）
- 按日期查看订餐统计
- 显示总订单数和菜品统计

#### 2.2.7 订餐设置（kitchen/admin）
- 设置早中晚三餐的截止时间
- 设置早中晚三餐的开饭时间

#### 2.2.8 用户管理（admin）
- 查看单位人员列表
- 添加/编辑/删除人员
- 设置人员角色

#### 2.2.9 个人中心
- 用户卡片展示（头像、姓名、手机号、角色）
- 提醒设置（订餐提醒、用餐提醒）
- 订餐统计（近30天/总计）
- 使用帮助

#### 2.2.10 订阅消息
- 订餐成功后发送确认通知
- 可在个人中心开启/关闭提醒

#### 2.2.11 菜品评价
- 已完成的订单可进行评价
- 支持星级评分和文字评论

### 2.3 技术要求

- 使用微信小程序云开发，无需搭建服务器
- 数据存储在云数据库中
- 支持百人以内的用户量
- 响应式设计，适配不同手机屏幕

## 3. 技术实现

### 3.1 技术栈

| 类别 | 技术 |
|------|------|
| 前端 | 微信小程序原生开发 (WXML + WXSS + JS) |
| 后端 | 微信云开发 (云函数、云数据库) |
| 订阅消息 | 微信订阅消息 API |
| 身份验证 | 微信手机号授权 + 单位人员列表 |

### 3.2 文件结构

```
miniprogram/
├── app.js                    # 应用入口（初始化、云函数调用）
├── app.json                  # 应用配置（页面路由、tabBar、窗口配置）
├── app.wxss                  # 全局样式
├── config.js                 # 配置文件（订阅消息模板ID）
├── utils/                    # 工具类目录
│   ├── auth.js              # 权限检查工具
│   ├── dateUtil.js          # 日期工具
│   └── initUtil.js          # 异步初始化工具
├── pages/                   # 页面目录
│   ├── auth/                # 身份验证页
│   ├── order/               # 订餐页（统一页面）
│   ├── history/             # 历史订单页
│   ├── profile/             # 个人中心页
│   ├── admin/               # 管理后台入口
│   ├── menu/                # 菜单管理页
│   ├── orderList/           # 订餐名单页
│   ├── statistics/          # 数据统计页
│   ├── settings/           # 订餐设置页
│   ├── userManage/         # 用户管理页
│   ├── evaluate/            # 评价页面
│   ├── reminderSettings/   # 提醒设置页
│   ├── orderStats/          # 订餐统计页
│   └── help/               # 使用帮助页
└── images/                  # 图片目录
    └── icons/              # tabBar图标

cloudfunctions/
├── quickstartFunctions/     # 通用云函数
│   └── index.js            # getOpenId, getPhoneNumber, getStaffList,
│                           # addStaff, updateStaff, deleteStaff,
│                           # createCanteenCollections, updateDeadlineConfig,
│                           # getDeadlineConfig, cancelOrder, getOrderStats
├── orderFunctions/          # 订单相关云函数
│   └── index.js            # getMenu, submitOrder, getOrderList,
│                           # getStatistics, submitEvaluation
└── sendSubscribeMessage/   # 订阅消息发送云函数
    └── index.js            # 发送微信订阅消息
```

### 3.3 数据库设计

#### 3.3.1 users（用户集合 - 已验证用户）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 手机号
  name: "张三",                    // 员工姓名
  isVerified: true,               // 是否已通过身份校验
  role: "staff",                  // 角色：staff/admin/kitchen
  userInfo: {},                   // 微信用户信息
  subscribeOrderReminder: true,   // 订餐提醒开关
  subscribeMealReminder: true,    // 用餐提醒开关
  createTime: Date,
  updateTime: Date
}
```

#### 3.3.2 staffs（单位人员列表）

```javascript
{
  _id: "自动生成",
  phone: "13800138000",         // 手机号（唯一标识）
  name: "张三",                  // 姓名
  role: "staff",                // 角色：staff/admin/kitchen
  status: "active",             // 状态：active/inactive
  createTime: Date,
  updateTime: Date
}
```

#### 3.3.3 menus（菜单集合）

```javascript
{
  _id: "自动生成",
  date: "2026-04-03",           // 日期，格式 YYYY-MM-DD
  meals: [                       // 餐次列表
    {
      type: "早餐",              // 餐次类型
      dishes: [                  // 菜品列表
        { name: "豆浆" },
        { name: "油条" }
      ]
    },
    {
      type: "午餐",
      dishes: [
        { name: "红烧肉" },
        { name: "青菜" }
      ]
    }
  ],
  createTime: Date,
  updateTime: Date
}
```

#### 3.3.4 orders（订单集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 手机号（用于用户识别）
  date: "2026-04-03",            // 订餐日期
  mealType: "早餐",              // 餐次类型：早餐/午餐/晚餐
  dishes: [                      // 菜品列表
    { name: "豆浆" },
    { name: "油条" }
  ],
  status: "pending",             // 状态：pending/cancelled/completed
  evaluated: false,              // 是否已评价
  createTime: Date,
  updateTime: Date
}
```

**订单状态说明**：

| 状态 | 说明 |
|------|------|
| pending | 待用餐（默认值） |
| cancelled | 已取消（用户主动取消或过期取消） |
| completed | 已完成（过开饭时间后前端计算显示） |

#### 3.3.5 evaluations（评价集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  orderId: "订单id",
  dishName: "豆浆",              // 菜品名称
  mealType: "早餐",             // 餐次类型
  rating: 5,                    // 评分 1-5
  comment: "很好吃",            // 评价内容
  createTime: Date
}
```

#### 3.3.6 configs（配置集合）

```javascript
{
  _id: "自动生成",
  key: "order_deadline",        // 配置键名
  breakfast_deadline: "08:00",   // 早餐截止时间
  lunch_deadline: "12:00",      // 午餐截止时间
  dinner_deadline: "17:00",     // 晚餐截止时间
  breakfast_meal_start: "08:00", // 早餐开饭时间
  lunch_meal_start: "12:00",     // 午餐开饭时间
  dinner_meal_start: "17:30",   // 晚餐开饭时间
  updateTime: Date
}
```

## 4. 界面设计

### 4.1 风格规范

- **主色调**：蓝色 (#1890FF)
- **背景色**：浅灰 (#F5F7FA)
- **卡片圆角**：16rpx
- **图标风格**：emoji 或线性图标

### 4.2 tabBar 配置

| 入口 | 页面 | 图标 |
|------|------|------|
| 订餐 | pages/order/order | home.png |
| 订单 | pages/history/history | business.png |
| 我的 | pages/profile/profile | usercenter.png |

### 4.3 页面导航

| 页面类型 | 导航方式 | 页面 |
|----------|----------|------|
| tabBar | 底部导航 | 订餐、订单、我的 |
| 管理端 | navigateTo | admin, menu, orderList, statistics, settings, userManage |
| 功能页 | navigateTo | evaluate, reminderSettings, orderStats, help |
| 验证页 | reLaunch | auth |

## 5. 接口定义

### 5.1 订阅消息模板

模板ID（在 config.js 中配置）：
```
tZAu7qUAcWS1T6d99m7elTaOkFZpi2VudxQu5pqxM70
```

### 5.2 订阅消息内容格式

```javascript
{
  thing1: { value: "2026-04-03 午餐" },  // 订单信息
  thing2: { value: "豆浆、油条" },         // 菜品列表
  thing3: { value: "请按时用餐" }          // 温馨提示
}
```

## 6. 业务流程

### 6.1 用户首次使用流程

```
打开小程序
    ↓
无 openid → 调用云函数获取
    ↓
检查 users 集合是否有记录
    ↓
无记录 → 跳转身份验证页
    ↓
选择验证方式（手机号授权/手动输入）
    ↓
验证手机号是否在 staffs 列表
    ↓
验证成功 → 创建/更新 users 记录
    ↓
进入订餐页面
```

### 6.2 订餐流程

```
进入订餐页面
    ↓
加载当日菜单（menus集合）
    ↓
加载用户当日订单（orders集合，按phone过滤）
    ↓
显示菜品列表和已选状态
    ↓
用户选择菜品
    ↓
点击"订餐"按钮
    ↓
请求订阅消息授权（可选）
    ↓
提交订单到 orders 集合
    ↓
发送订阅消息通知（如果已授权）
    ↓
刷新页面显示已订状态
```

### 6.3 订单状态转换

```
pending（待用餐）
    ↓
    ├─ 用户取消 → cancelled（截止时间前可取消）
    ├─ 过开饭时间 → completed（前端计算，不入库）
    └─ 标记已评价 → evaluated: true
```

## 7. 项目进度

| 阶段 | 状态 |
|------|------|
| 需求分析 | ✅ 已完成 |
| 数据库设计 | ✅ 已完成 |
| 前端开发 | ✅ 已完成 |
| 后端开发 | ✅ 已完成 |
| 测试 | ✅ 已完成 |
| 部署 | ⏳ 待部署 |

## 8. 注意事项

### 8.1 环境要求

- 微信开发者工具 2.2.3+
- 微信小程序基础库 2.2.3+

### 8.2 部署要求

- 需在微信公众平台注册小程序并获取 AppID
- 需在微信开发者工具中初始化云开发环境
- 需配置订阅消息模板（获取模板ID）
- 需在云开发控制台创建相应数据库集合（或通过初始化功能）

### 8.3 初始配置

1. 部署 quickstartFunctions、orderFunctions、sendSubscribeMessage 云函数
2. 使用管理员手机号登录小程序完成首次验证
3. 在 staffs 集合中确保有该管理员记录（role: 'admin'）

## 9. 版本更新记录

### v1.3（最新版本）

- **用户卡片**：个人中心展示头像、姓名、加密手机号、角色标签
- **提醒设置**：支持订餐提醒和用餐提醒开关
- **订餐统计**：查看近30天和总计订餐次数
- **使用帮助**：按角色展示功能说明
- **手机号标识**：订单查询改用 phone 字段

### v1.2

- 身份验证功能
- 多角色支持
- 用户管理
- 订餐页面重构

### v1.1

- 订单以顿为单位
- 支持取消预约
- 日期工具类

### v1.0

- 基础功能实现
