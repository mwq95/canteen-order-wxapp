# 食堂订餐小程序大模型提示

## 项目概述
这是一个单位食堂订餐微信小程序，用于统计用餐需求，方便员工订餐和食堂管理。

## 核心功能
1. **菜单展示**：展示当日和次日的菜单，包括早中晚餐
2. **订餐功能**：用户可以选择菜品进行订餐，支持多次修改订单
3. **微信提醒**：使用微信订阅消息发送订餐成功提醒
4. **菜品评价**：用户可以对已用餐品进行星级评分
5. **后台管理**：管理员可以设置每日菜单、查看订餐名单和数据分析
6. **数据分析**：提供每日订餐人数、菜品受欢迎程度和订餐时间分布统计

## 技术实现
- 前端：微信小程序原生开发
- 后端：微信小程序云开发
- 数据库：云数据库

## 项目结构
- `pages/`：页面目录（8个页面）
- `cloudfunctions/`：云函数目录（6个云函数）
- `images/`：图片目录（小程序图标和tabBar图标）
- `app.js`、`app.json`、`app.wxss`：小程序全局配置和样式

## 数据库结构
- **users**：用户表，存储用户信息
- **menus**：菜单表，存储每日菜单
- **orders**：订单表，存储用户订餐记录
- **evaluations**：评价表，存储菜品评价
- **configs**：配置表，存储系统配置

## 关键文件说明
1. **app.json**：小程序配置文件，包含页面路径、窗口配置、tabBar配置等
2. **pages/index/index.js**：首页逻辑，调用getMenu云函数获取菜单数据
3. **pages/order/order.js**：订餐页面逻辑，调用submitOrder云函数提交订单
4. **pages/evaluate/evaluate.js**：评价页面逻辑，调用submitEvaluation云函数提交评价
5. **pages/statistics/statistics.js**：数据分析页面逻辑，调用getStatistics云函数获取统计数据
6. **cloudfunctions/getMenu/index.js**：获取菜单数据的云函数
7. **cloudfunctions/submitOrder/index.js**：提交订单的云函数
8. **cloudfunctions/getStatistics/index.js**：获取数据分析的云函数

## 使用说明
1. **部署云函数**：在微信开发者工具中右键点击cloudfunctions目录，选择"上传并部署所有云函数"
2. **初始化数据库**：在云开发控制台创建users、menus、orders、evaluations、configs集合
3. **配置订阅消息**：在微信公众平台申请订阅消息模板，并在configs集合中配置模板ID
4. **测试功能**：在微信开发者工具中编译并测试所有功能

## 注意事项
- 需要在微信公众平台注册小程序并获取AppID
- 需要在微信开发者工具中初始化云开发环境
- 首次使用时需要在云开发控制台创建数据库集合
- 订阅消息需要用户主动授权

## 扩展建议
- 可以添加更多菜品信息，如营养成分、食材来源等
- 可以实现菜品分类和搜索功能
- 可以添加批量订餐和团队订餐功能
- 可以实现在线支付功能
- 可以添加食堂公告和活动通知功能