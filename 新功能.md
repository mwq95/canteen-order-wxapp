# 个人中心新功能实现文档

## 一、功能概述

对个人中心页面（profile）进行功能扩展，新增以下四个模块：

| 模块 | 说明 |
|------|------|
| 用户卡片 | 展示微信头像、员工姓名、加密手机号 |
| 提醒设置 | 二级页面，包含订餐提醒和用餐提醒两个开关 |
| 订餐统计 | 本周/本月/本年/总计的订餐数据统计 |
| 使用帮助 | 分角色展示系统功能说明 |

## 二、整体页面布局

```
┌─────────────────────────────────────┐
│                                     │
│  ┌───────────────────────────────┐  │
│  │         【用户卡片】            │  │
│  │   [头像]  员工姓名             │  │
│  │           手机号(138****8888)   │  │
│  │           角色: 工作人员       │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 🔔 提醒设置               ›  │  │
│  ├───────────────────────────────┤  │
│  │ 📊 订餐统计               ›  │  │
│  ├───────────────────────────────┤  │
│  │ ❓ 使用帮助               ›  │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ ⚙️ 管理后台 (仅管理员可见)    │  │
│  └───────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

---

## 三、模块一：用户卡片

### 3.1 数据来源

| 字段 | 来源 | 获取方式 |
|------|------|---------|
| 微信头像 | `app.globalData.userInfo.avatarUrl` | app.js 初始化时获取，或使用默认图 `/images/avatar.png` |
| 员工姓名 | 需新增：通过手机号关联 staffs 查询 | 云函数查询或前端直接查 |
| 手机号 | `app.globalData.phone` | auth.js 验证后存入 globalData |
| 用户角色 | `app.globalData.role` | auth.js 验证后存入 globalData |

### 3.2 手机号加密规则

```
原始号码: 13812345678
显示格式: 138****5678
规则:     前3位 + **** + 后4位
```

### 3.3 员工姓名获取方案

**方案：在 users 集合中冗余存储姓名**

auth.js 的 `createOrUpdateUser` 方法中，验证成功后将员工姓名写入 users 集合：

```javascript
// auth.js - createOrUpdateUser 方法中补充
const userData = {
  phone: staff.phone,
  name: staff.name,        // 新增：员工姓名
  isVerified: true,
  role: staff.role,
  updateTime: new Date()
};
```

同时在 app.js 的 `checkUserStatus` 中读取：

```javascript
// app.js - checkUserStatus 补充
this.globalData.name = user.name || null;  // 新增
```


### 3.4 WXML 结构

```xml
<view class="user-card">
  <view class="user-info">
    <image 
      class="avatar" 
      src="{{userInfo.avatarUrl || '/images/avatar.png'}}"
      mode="aspectFill"
    ></image>
    <view class="user-detail">
      <view class="nickname">{{staffName || '未设置'}}</view>
      <view class="phone">{{maskedPhone}}</view>
      <view class="role-tag">{{roleText}}</view>
    </view>
  </view>
</view>
```

### 3.5 JS 逻辑

```javascript
data: {
  userInfo: null,
  staffName: '',
  maskedPhone: '',
  roleText: '',
  canAccessAdmin: false
},

async onShow() {
  if (auth.isInitializing()) {
    await initUtil.waitForAppInit();
  }
  auth.checkPageAccess('tabbar');
  this.loadUserInfo();
  this.checkAdminAccess();
},

loadUserInfo() {
  const app = getApp();
  const phone = app.globalData.phone;
  
  // 姓名从 globalData 或 users 集合获取
  const staffName = app.globalData.name || '未录入';
  
  // 手机号加密
  const maskedPhone = phone ? phone.replace(/(\d{3})\d{4}(\d{4})/, '$1****$2') : '未绑定';
  
  // 角色文字映射
  const roleMap = { admin: '管理员', kitchen: '厨房', staff: '工作人员' };
  const roleText = roleMap[app.globalData.role] || '工作人员';
  
  // 头像信息
  const userInfo = app.globalData.userInfo || {};
  
  this.setData({
    userInfo,
    staffName,
    maskedPhone,
    roleText
  });
}
```

### 3.6 WXSS 样式

```css
.user-card {
  background: linear-gradient(135deg, #1890FF 0%, #096DD9 100%);
  border-radius: 20rpx;
  padding: 50rpx 40rpx;
  margin-bottom: 30rpx;
}

.user-info {
  display: flex;
  align-items: center;
  gap: 28rpx;
}

.avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
  background: rgba(255, 255, 255, 0.2);
}

.user-detail {
  flex: 1;
}

.nickname {
  font-size: 36rpx;
  font-weight: bold;
  color: #fff;
  margin-bottom: 8rpx;
}

.phone {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.85);
  margin-bottom: 6rpx;
}

.role-tag {
  display: inline-block;
  font-size: 22rpx;
  color: #fff;
  background: rgba(255, 255, 255, 0.25);
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
}
```

---

## 四、模块二：提醒设置

### 4.1 功能说明

点击"提醒设置"进入二级页面，包含两个开关：
- **订餐提醒**：订餐成功后发送微信订阅消息通知
- **用餐提醒**：开饭时间到达前发送微信订阅消息通知

### 4.2 数据存储

在 `users` 集合中新增字段：

```javascript
{
  _openid: "xxx",
  subscribeOrderReminder: true,    // 订餐提醒开关
  subscribeMealReminder: true,    // 用餐提醒开关
  // ... 其他字段
}
```

### 4.3 页面文件

新建页面：`miniprogram/pages/reminderSettings/reminderSettings`

#### WXML

```xml
<view class="container">
  <view class="header">
    <text class="title">提醒设置</text>
    <text class="subtitle">开启后将接收对应的微信消息通知</text>
  </view>

  <view class="setting-list">
    <!-- 订餐提醒 -->
    <view class="setting-item">
      <view class="setting-left">
        <text class="setting-icon">📋</text>
        <view class="setting-text">
          <text class="setting-name">订餐提醒</text>
          <text class="setting-desc">订餐成功后收到确认通知</text>
        </view>
      </view>
      <switch 
        checked="{{orderReminder}}" 
        bindchange="onOrderReminderChange"
        color="#1890FF"
      />
    </view>

    <!-- 用餐提醒 -->
    <view class="setting-item">
      <view class="setting-left">
        <text class="setting-icon">🍽️</text>
        <view class="setting-text">
          <text class="setting-name">用餐提醒</text>
          <text class="setting-desc">开饭时间到达前收到提醒</text>
        </view>
      </view>
      <switch 
        checked="{{mealReminder}}" 
        bindchange="onMealReminderChange"
        color="#1890FF"
      />
    </view>
  </view>

  <view class="tips-card">
    <text class="tips-title">温馨提示</text>
    <text class="tips-text">1. 开启提醒需要您授权微信订阅消息权限</text>
    <text class="tips-text">2. 如未授权，首次触发时会弹出授权弹窗</text>
    <text class="tips-text">3. 您可以在微信设置中管理订阅消息</text>
  </view>
</view>
```

#### JS

```javascript
const app = getApp();

Page({
  data: {
    orderReminder: true,
    mealReminder: true
  },

  onLoad() {
    this.loadSettings();
  },

  loadSettings() {
    const db = wx.cloud.database();
    db.collection('users').where({
      _openid: app.globalData.openid
    }).get().then(res => {
      if (res.data.length > 0) {
        const user = res.data[0];
        this.setData({
          orderReminder: user.subscribeOrderReminder !== false,
          mealReminder: user.subscribeMealReminder !== false
        });
      }
    });
  },

  onOrderReminderChange(e) {
    const value = e.detail.value;
    if (value) {
      this.requestSubscribeAndSave('order');
    } else {
      this.saveSetting({ subscribeOrderReminder: false });
    }
    this.setData({ orderReminder: value });
  },

  onMealReminderChange(e) {
    const value = e.detail.value;
    if (value) {
      this.requestSubscribeAndSave('meal');
    } else {
      this.saveSetting({ subscribeMealReminder: false });
    }
    this.setData({ mealReminder: value });
  },

  requestSubscribeAndSave(type) {
    const templateId = wx.getStorageSync('subscribeTemplateId') || '';
    
    if (!templateId) {
      wx.showToast({ title: '模板ID未配置', icon: 'none' });
      return;
    }

    wx.requestSubscribeMessage({
      tmplIds: [templateId],
      success: (res) => {
        if (res[templateId] === 'accept') {
          if (type === 'order') {
            this.saveSetting({ subscribeOrderReminder: true });
          } else {
            this.saveSetting({ subscribeMealReminder: true });
          }
          wx.showToast({ title: '已开启', icon: 'success' });
        } else {
          if (type === 'order') {
            this.setData({ orderReminder: false });
          } else {
            this.setData({ mealReminder: false });
          }
          wx.showToast({ title: '已拒绝', icon: 'none' });
        }
      },
      fail: () => {
        if (type === 'order') {
          this.setData({ orderReminder: false });
        } else {
          this.setData({ mealReminder: false });
        }
      }
    });
  },

  saveSetting(data) {
    const db = wx.cloud.database();
    db.collection('users').where({
      _openid: app.globalData.openid
    }).update({
      data: {
        ...data,
        updateTime: new Date()
      }
    });
  }
});
```

#### WXSS

```css
.container {
  padding: 20rpx;
  min-height: 100vh;
  background: #f5f7fa;
}

.header {
  text-align: center;
  margin-bottom: 40rpx;
  padding-top: 20rpx;
}

.title {
  font-size: 38rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 10rpx;
}

.subtitle {
  font-size: 26rpx;
  color: #999;
}

.setting-list {
  background: #fff;
  border-radius: 16rpx;
  overflow: hidden;
}

.setting-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 30rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.setting-item:last-child {
  border-bottom: none;
}

.setting-left {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.setting-icon {
  font-size: 40rpx;
}

.setting-text {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.setting-name {
  font-size: 30rpx;
  color: #333;
  font-weight: 500;
}

.setting-desc {
  font-size: 24rpx;
  color: #999;
}

.tips-card {
  margin-top: 30rpx;
  background: #fffbe6;
  border-radius: 16rpx;
  padding: 24rpx 30rpx;
}

.tips-title {
  font-size: 28rpx;
  font-weight: bold;
  color: #faad14;
  display: block;
  margin-bottom: 12rpx;
}

.tips-text {
  font-size: 24rpx;
  color: #666;
  line-height: 1.8;
  display: block;
}
```

### 4.4 与订餐流程的联动

在 `order.js` 的 `doSubmitOrder` 方法中，判断是否发送提醒：

```javascript
// order.js - doSubmitOrder 中修改
if (hasSubscribed && !existingOrder.orderId) {
  // 检查用户的订餐提醒设置
  this.checkAndSendReminder(mealType, selections);
}

checkAndSendReminder(mealType, selections) {
  const db = wx.cloud.database();
  db.collection('users').where({
    _openid: app.globalData.openid
  }).field({ subscribeOrderReminder: true }).get().then(res => {
    if (res.data.length > 0 && res.data[0].subscribeOrderReminder !== false) {
      this.sendSubscribeMessage(mealType, selections);
    }
  });
}
```

---

## 五、模块三：订餐统计

### 5.1 功能说明

展示用户的订餐数据统计，分为两个维度：
- **近30天**：最近30天内的订餐次数
- **总计**：所有时间的累计订餐次数

每个维度只显示一个指标：**订餐次数**（不区分完成/取消，所有订单都计入）

### 5.2 页面文件

新建页面：`miniprogram/pages/orderStats/orderStats`

#### WXML

```xml
<view class="container">
  <view class="header">
    <text class="title">订餐统计</text>
  </view>

  <!-- 近30天 -->
  <view class="stats-card">
    <view class="stats-header">
      <text class="stats-label">📅 近30天</text>
      <text class="stats-date">{{thirtyDayRange}}</text>
    </view>
    <view class="stats-row single">
      <view class="stat-item">
        <text class="stat-num">{{thirtyDayCount}}</text>
        <text class="stat-label">次</text>
      </view>
    </view>
  </view>

  <!-- 总计 -->
  <view class="stats-card highlight">
    <view class="stats-header">
      <text class="stats-label">🏆 总计</text>
    </view>
    <view class="stats-row single">
      <view class="stat-item">
        <text class="stat-num total-big">{{totalCount}}</text>
        <text class="stat-label">次</text>
      </view>
    </view>
  </view>
</view>
```

#### JS

```javascript
const app = getApp();

Page({
  data: {
    thirtyDayRange: '',
    thirtyDayCount: 0,
    totalCount: 0
  },

  onLoad() {
    this.setDateRanges();
    this.loadStats();
  },

  setDateRanges() {
    const now = new Date();
    const thirtyDaysAgo = new Date(now);
    thirtyDaysAgo.setDate(now.getDate() - 30);

    const formatDate = (d) => {
      return `${d.getMonth() + 1}/${d.getDate()}`;
    };

    this.setData({
      thirtyDayRange: `${formatDate(thirtyDaysAgo)} ~ ${formatDate(now)}`
    });
  },

  async loadStats() {
    await Promise.all([
      this.loadThirtyDayStats(),
      this.loadTotalStats()
    ]);
  },

  async loadThirtyDayStats() {
    const db = wx.cloud.database();
    const now = new Date();
    const thirtyDaysAgo = new Date(now);
    thirtyDaysAgo.setDate(now.getDate() - 30);
    thirtyDaysAgo.setHours(0, 0, 0, 0);

    const res = await db.collection('orders')
      .where({
        _openid: app.globalData.openid,
        date: db.command.gte(this.formatDate(thirtyDaysAgo)).and(db.command.lte(this.formatDate(now)))
      })
      .count();

    this.setData({ thirtyDayCount: res.total });
  },

  async loadTotalStats() {
    const db = wx.cloud.database();
    const res = await db.collection('orders')
      .where({
        _openid: app.globalData.openid
      })
      .count();

    this.setData({ totalCount: res.total });
  },

  formatDate(date) {
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
  }
});
```

#### WXSS

```css
.container {
  padding: 20rpx;
  min-height: 100vh;
  background: #f5f7fa;
}

.header {
  text-align: center;
  margin-bottom: 30rpx;
  padding-top: 20rpx;
}

.title {
  font-size: 38rpx;
  font-weight: bold;
  color: #333;
}

.stats-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 40rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.05);
}

.stats-card.highlight {
  background: linear-gradient(135deg, #e6f7ff 0%, #bae7ff 100%);
  border: 2rpx solid #91d5ff;
}

.stats-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.stats-label {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
}

.stats-date {
  font-size: 24rpx;
  color: #999;
}

.stats-row.single {
  justify-content: center;
}

.stat-item {
  display: flex;
  flex-direction: row;
  align-items: baseline;
  gap: 8rpx;
}

.stat-num {
  font-size: 64rpx;
  font-weight: bold;
  color: #333;
}

.stat-num.total-big {
  font-size: 72rpx;
  color: #1890FF;
}

.stat-label {
  font-size: 28rpx;
  color: #999;
}
```

### 5.3 性能优化（可选）

如需进一步优化，可将查询逻辑移至云函数：

```javascript
// cloudfunctions/quickstartFunctions/index.js 新增
const getOrderStats = async (event, openid) => {
  const db = cloud.database();
  const now = new Date();
  const thirtyDaysAgo = new Date(now);
  thirtyDaysAgo.setDate(now.getDate() - 30);

  const [thirtyDayRes, totalRes] = await Promise.all([
    db.collection('orders')
      .where({
        _openid: openid,
        date: db.command.gte(formatDate(thirtyDaysAgo)).and(db.command.lte(formatDate(now)))
      })
      .count(),
    db.collection('orders')
      .where({ _openid: openid })
      .count()
  ]);

  return {
    success: true,
    data: {
      thirtyDay: thirtyDayRes.total,
      total: totalRes.total
    }
  };
};
```

---

## 六、模块四：使用帮助

### 6.1 功能说明

根据用户角色展示对应的功能帮助文档。不同角色看到的内容不同：

| 角色 | 可见内容 |
|------|---------|
| **staff（工作人员）** | 订餐、查看订单、评价、个人设置 |
| **kitchen（厨房）** | 以上 + 菜单管理 |
| **admin（管理员）** | 以上 + 人员管理、系统设置 |

### 6.2 页面文件

新建页面：`miniprogram/pages/help/help`

#### WXML

```xml
<view class="container">
  <view class="header">
    <text class="title">使用帮助</text>
    <text class="subtitle">了解系统各项功能的使用方法</text>
  </view>

  <!-- 基础功能（所有角色可见） -->
  <view class="section">
    <view class="section-title">基础功能</view>

    <view class="help-item">
      <view class="help-q">如何订餐？</view>
      <view class="help-a">
        1. 进入「订餐」页面\n
        2. 选择日期查看当日菜单\n
        3. 点击菜品进行选择（选中会高亮显示）\n
        4. 点击底部「订餐」按钮提交\n
        5. 订餐成功后会收到微信通知（如已开启）
      </view>
    </view>

    <view class="help-item">
      <view class="help-q">如何修改或取消订单？</view>
      <view class="help-a">
        • 进入「我的订单」页面\n
        • 在截止时间前可点击「取消预约」\n
        • 截止时间后无法取消，但可以修改菜品\n
        • 过了开饭时间后订单变为已完成状态
      </view>
    </view>

    <view class="help-item">
      <view class="help-q">什么是订餐截止时间和开饭时间？</view>
      <view class="help-a">
        • 订餐截止时间：在此时间前可以订餐和取消\n
        • 开饭时间：过了此时间订单自动标记为已完成\n
        • 具体时间由管理员在设置页配置
      </view>
    </view>

    <view class="help-item">
      <view class="help-q">如何对餐品进行评价？</view>
      <view class="help-a">
        • 开饭时间后，订单状态变为「已完成」\n
        • 此时会出现「评价」按钮\n
        → 点击即可进入评价页面打分和留言
      </view>
    </view>
  </view>

  <!-- 厨房功能（厨房和管理员可见） -->
  <view class="section" wx:if="{{showKitchen}}">
    <view class="section-title">厨房功能</view>

    <view class="help-item">
      <view class="help-q">如何管理菜单？</view>
      <view class="help-a">
        • 进入管理后台 → 菜单管理\n
        • 选择日期添加或编辑当日菜单\n
        • 为每餐（早/午/晚）添加菜品名称\n
        • 支持添加多个菜品
      </view>
    </view>
  </view>

  <!-- 管理员功能（仅管理员可见） -->
  <view class="section" wx:if="{{showAdmin}}">
    <view class="section-title">管理功能</view>

    <view class="help-item">
      <view class="help-q">如何管理人员？</view>
      <view class="help-a">
        • 进入管理后台 → 人员管理\n
        • 点击「添加人员」录入新员工信息\n
        • 设置员工的角色（管理员/工作人员/厨房）\n
        • 支持编辑和停用操作
      </view>
    </view>

    <view class="help-item">
      <view class="help-q">如何设置订餐参数？</view>
      <view class="help-a">
        • 进入管理后台 → 订餐设置\n
        • 设置各餐的截止订餐时间\n
        • 设置各餐的开饭时间\n
        • 保存后立即生效
      </view>
    </view>

    <view class="help-item">
      <view class="help-q">如何初始化数据库？</view>
      <view class="help-a">
        • 首次部署或数据异常时可能需要初始化\n
        • 订餐页或首页出现提示时点击「初始化数据库」\n
        • 将创建所需的数据库集合\n
        • 注意：不会清除已有数据
      </view>
    </view>
  </view>

  <!-- 关于信息 -->
  <view class="footer">
    <text class="version">版本号：v1.0.0</text>
  </view>
</view>
```

#### JS

```javascript
const auth = require('../../utils/auth.js');

Page({
  data: {
    showKitchen: false,
    showAdmin: false
  },

  onLoad() {
    const role = auth.getRole();
    this.setData({
      showKitchen: role === 'admin' || role === 'kitchen',
      showAdmin: role === 'admin'
    });
  }
});
```

#### WXSS

```css
.container {
  padding: 20rpx;
  min-height: 100vh;
  background: #f5f7fa;
}

.header {
  text-align: center;
  margin-bottom: 36rpx;
  padding-top: 16rpx;
}

.title {
  font-size: 38rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 8rpx;
}

.subtitle {
  font-size: 26rpx;
  color: #999;
}

.section {
  background: #fff;
  border-radius: 16rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.05);
}

.section-title {
  font-size: 30rpx;
  font-weight: bold;
  color: #1890FF;
  margin-bottom: 20rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.help-item {
  margin-bottom: 24rpx;
}

.help-item:last-child {
  margin-bottom: 0;
}

.help-q {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 12rpx;
  display: block;
}

.help-a {
  font-size: 26rpx;
  color: #666;
  line-height: 1.8;
  white-space: pre-line;
  padding-left: 16rpx;
  border-left: 4rpx solid #91d5ff;
  padding: 16rpx 16rpx 16rpx 24rpx;
  background: #fafafa;
  border-radius: 0 8rpx 8rpx 0;
}

.footer {
  text-align: center;
  padding: 40rpx 0;
}

.version {
  font-size: 24rpx;
  color: #ccc;
}
```

---

## 七、路由配置

需要在 `app.json` 中注册三个新页面：

```json
{
  "pages": [
    "...",
    "pages/profile/profile",
    "pages/reminderSettings/reminderSettings",
    "pages/orderStats/orderStats",
    "pages/help/help"
  ]
}
```

## 八、profile.wxml 更新

更新后的 profile 页面主结构：

```xml
<view class="container">
  <!-- 用户卡片 -->
  <view class="user-card">
    <view class="user-info">
      <image class="avatar" src="{{userInfo.avatarUrl || '/images/avatar.png'}}" mode="aspectFill"></image>
      <view class="user-detail">
        <view class="nickname">{{staffName || '未录入'}}</view>
        <view class="phone">{{maskedPhone}}</view>
        <view class="role-tag">{{roleText}}</view>
      </view>
    </view>
  </view>

  <!-- 功能列表 -->
  <view class="menu-list">
    <view class="menu-item" bindtap="goToReminder">
      <view class="menu-icon">🔔</view>
      <view class="menu-text">提醒设置</view>
      <view class="menu-arrow">›</view>
    </view>

    <view class="menu-item" bindtap="goToStats">
      <view class="menu-icon">📊</view>
      <view class="menu-text">订餐统计</view>
      <view class="menu-arrow">›</view>
    </view>

    <view class="menu-item" bindtap="goToHelp">
      <view class="menu-icon">❓</view>
      <view class="menu-text">使用帮助</view>
      <view class="menu-arrow">›</view>
    </view>

    <view wx:if="{{canAccessAdmin}}" class="menu-item" bindtap="goToAdmin">
      <view class="menu-icon">⚙️</view>
      <view class="menu-text">管理后台</view>
      <view class="menu-arrow">›</view>
    </view>
  </view>
</view>
```

## 九、开发顺序建议

| 步骤 | 内容 | 预估耗时 | 依赖 |
|------|------|---------|------|
| 1 | 用户卡片改造 | 15分钟 | 无 |
| 2 | 提醒设置页面 | 45分钟 | 无 |
| 3 | 订餐统计页面 | 60分钟 | 可能需要云函数优化 |
| 4 | 使用帮助页面 | 30分钟 | 无 |
| 5 | 路由注册与联调 | 10分钟 | 以上全部完成 |

总计预估约 2.5 小时。

---

## 十、以手机号替代 openid 作为用户标识

### 10.1 改动背景

**现有方案**：使用 `_openid`（微信生成的设备唯一标识）区分用户
**问题**：一个用户可能有多个微信，或者换手机后 openid 变化，导致：
- 同一手机号对应多个 openid，数据分散
- 订餐记录无法合并统计
- 切换账号后历史订单丢失

**新方案**：以 `phone`（手机号）作为用户唯一标识，确保：
- 同一个手机号无论哪个微信登录，订单数据统一
- 一个手机号只有一个订餐统计
- 更换手机不影响订餐记录

### 10.2 涉及改动的文件清单

| 序号 | 文件路径 | 改动内容 | 改动类型 | 风险 |
|------|---------|---------|---------|------|
| 1 | `cloudfunctions/quickstartFunctions/index.js` | `cancelOrder` 方法中 `order._openid !== openid` 改为 `order.phone !== phone`，且 `phone` 从 `event` 参数传入 | 中 | 需云函数重新部署 |
| 2 | `cloudfunctions/orderFunctions/index.js` | 查询/新增订单时用 `phone` 而非 `_openid` | 大 | 核心业务逻辑 |
| 3 | `cloudfunctions/quickstartFunctions/index.js` — `addStaff`/`updateStaff`/`deleteStaff` | 相关方法中也改用 `phone` | 中 | 需确认是否已有 phone 字段 |
| 4 | `miniprogram/app.js` — `checkUserStatus` | 将 `phone` 存入 `globalData`（已有） | 小 | 确认 globalData 结构 |
| 5 | `miniprogram/pages/order/order.js` — `loadData` | 查询订单条件从 `_openid` 改为 `phone` | 中 | 影响订餐页数据加载 |
| 6 | `miniprogram/pages/history/history.js` — `loadOrders` | 查询订单条件从 `_openid` 改为 `phone` | 中 | 影响历史订单 |
| 7 | `miniprogram/pages/orderStats/orderStats.js` — 新页面 | 统计查询用 `phone` 而非 `openid` | 小 | 新页面，无历史包袱 |
| 8 | `miniprogram/pages/reminderSettings/reminderSettings.js` | users 集合查询用 `phone` | 小 | 新页面 |
| 9 | `miniprogram/pages/auth/auth.js` — `createOrUpdateUser` | users 集合查询和写入用 `phone` 而非 `_openid` | 大 | 需同步历史数据 |
| 10 | `cloudfunctions/quickstartFunctions/index.js` — `updateDeadlineConfig`/`getDeadlineConfig` | 不涉及订单，**无需改动** | — | — |
| 11 | 数据库集合权限 | `orders` 集合的权限筛选条件从 `_openid` 改为 `phone` | 中 | 需同步更新权限规则 |

### 10.3 详细改动说明

#### 10.3.1 数据库集合改动

**orders 集合**：新增 `phone` 字段（必填），作为用户标识

```javascript
// 新增订单时
{
  phone: "13812345678",      // 新增：用户手机号（唯一标识）
  date: "2026-04-08",
  mealType: "午餐",
  dishes: [{ name: "红烧肉" }],
  status: "pending",
  createTime: Date,
  updateTime: Date
  // _openid 字段保留（微信自动生成，用于其他场景）
}
```

**users 集合**：将查询条件从 `_openid` 改为 `phone`

```javascript
// 原来：按 _openid 查询
db.collection('users').where({ _openid: app.globalData.openid })

// 改为：按 phone 查询
db.collection('users').where({ phone: app.globalData.phone })
```

#### 10.3.2 云函数改动

**cancelOrder 方法**：

```javascript
// 原来：通过 openid 验证权限
if (order._openid !== openid) {
  return { success: false, error: '无权操作此订单' };
}

// 改为：通过 phone 验证
const { phone } = event;
if (order.phone !== phone) {
  return { success: false, error: '无权操作此订单' };
}
```

**orderFunctions/index.js**：

```javascript
// 查询订单 - 原来
.where({ _openid: openid, date: selectedDate })

// 改为
.where({ phone: phone, date: selectedDate })

// 新增订单 - 原来
db.collection('orders').add({
  data: { _openid: openid, date, mealType, dishes, status: 'pending' }
})

// 改为
db.collection('orders').add({
  data: { phone: phone, date, mealType, dishes, status: 'pending' }
})
```

#### 10.3.3 前端页面改动

**order.js — loadData**：

```javascript
// 原来
db.collection('orders').where({
  _openid: app.globalData.openid,
  date
}).get()

// 改为
db.collection('orders').where({
  phone: app.globalData.phone,
  date
}).get()
```

**history.js — loadOrders**：

```javascript
// 原来
db.collection('orders').where({
  _openid: app.globalData.openid
}).orderBy('createTime', 'desc').get()

// 改为
db.collection('orders').where({
  phone: app.globalData.phone
}).orderBy('createTime', 'desc').get()
```

#### 10.3.4 集合权限改动

在微信开发者工具的「云开发控制台」中，需要修改 `orders` 集合的权限配置：

```
原来：
- 查询：_openid == openid（用户只能查自己的）
- 新增：openid（自动填充）
- 更新：_openid == openid（用户只能改自己的）
- 删除：_openid == openid（用户只能删自己的）

改为：
- 查询：phone == auth.openid（需自定义安全规则，或通过云函数）
- 新增：由云函数调用，传入真实 phone
- 更新：通过云函数验证 phone 匹配
- 删除：通过云函数验证 phone 匹配
```

> **建议**：所有 orders 的读写都通过云函数操作，前端不直接操作数据库，避免权限问题。

### 10.4 历史数据迁移方案

对于已存在的订单数据，需要将 `_openid` 映射到 `phone`。由于 `_openid` 无法反查手机号，需要分情况处理：

**方案A：基于 staffs 集合关联（推荐）**

`staffs` 集合中已有 `_openid` 和 `phone` 的对应关系，可以通过 join 查询批量更新：

```javascript
// 数据迁移脚本（云函数临时创建，使用后删除）
async function migrateOrders() {
  const staffs = await db.collection('staffs').get();
  const openidToPhone = {};
  staffs.data.forEach(s => {
    openidToPhone[s._openid] = s.phone;
  });

  const orders = await db.collection('orders').limit(1000).get();
  for (const order of orders.data) {
    const phone = openidToPhone[order._openid];
    if (phone && !order.phone) {
      await db.collection('orders').doc(order._id).update({
        data: { phone }
      });
    }
  }
}
```

**方案B：用户主动触发（用户打开小程序时自动补全）**

在 `checkUserStatus` 中，如果发现订单没有 `phone` 字段，自动补全：

```javascript
// app.js — checkUserStatus 末尾
if (app.globalData.phone && app.globalData.isVerified) {
  // 补全历史订单的 phone 字段
  const db = wx.cloud.database();
  db.collection('orders').where({
    _openid: app.globalData.openid,
    phone: null
  }).update({
    data: { phone: app.globalData.phone }
  });
}
```

### 10.5 改动优先级与风险评估

| 步骤 | 操作内容 | 风险 | 建议 |
|------|---------|------|------|
| 1 | 修改云函数 `cancelOrder` 支持 `phone` 参数 | 中 | 先改云函数，不影响现有流程 |
| 2 | 修改 `orderFunctions` 的订单查询/新增逻辑 | 高 | 核心改动，建议在低峰期操作 |
| 3 | 修改 `history.js` 和 `order.js` 的查询条件 | 中 | 配合云函数改动 |
| 4 | 修改 `auth.js` 中 users 集合的查询 | 中 | 影响认证流程，需测试 |
| 5 | 集合权限调整 | 高 | 必须通过云函数读写，避免前端直接操作 |
| 6 | 新功能页面（orderStats/reminderSettings） | 低 | 新增页面无历史包袱 |

### 10.6 改动预估工作量

| 内容 | 预估时间 |
|------|---------|
| 云函数改动（cancelOrder + orderFunctions） | 1小时 |
| 前端页面改动（order.js + history.js + auth.js） | 30分钟 |
| 集合权限调整 | 30分钟 |
| 测试验证 | 1小时 |
| **总计** | **约2.5小时** |

> 注意：此改动涉及核心业务逻辑，**强烈建议在测试环境充分验证后再部署生产环境**。
