# 食堂订餐小程序 - 项目开发文档

## 一、项目概述

### 1.1 项目简介
基于微信小程序的食堂订餐系统，支持菜单管理、在线订餐、订单管理、数据统计等功能。

### 1.2 技术栈
- **前端**：微信小程序原生开发 (WXML + WXSS + JS)
- **后端**：微信云开发 (云函数、云数据库、云存储)
- **订阅消息**：微信订阅消息

### 1.3 项目结构
```
miniprogram-1/
├── miniprogram/                 # 小程序前端代码
│   ├── app.js                  # 应用入口
│   ├── app.json                # 应用配置
│   ├── app.wxss                # 全局样式
│   ├── config.js               # 配置文件（订阅消息模板ID）
│   ├── pages/                  # 页面目录
│   │   ├── index/              # 首页
│   │   ├── order/              # 订餐页
│   │   ├── history/            # 历史订单
│   │   ├── profile/            # 个人中心
│   │   ├── admin/              # 管理后台入口
│   │   ├── menu/               # 菜单管理
│   │   ├── statistics/          # 数据统计
│   │   ├── evaluate/            # 评价页面
│   │   ├── orderList/           # 订餐名单
│   │   └── settings/            # 订餐设置
│   └── images/                  # 图片资源
├── cloudfunctions/              # 云函数目录
│   ├── quickstartFunctions/     # 通用云函数（openid获取、集合创建）
│   └── sendSubscribeMessage/   # 订阅消息发送云函数
└── 项目开发文档.md              # 本文档
```

---

## 二、数据库设计

### 2.1 集合列表
| 集合名 | 说明 | 权限建议 |
|--------|------|----------|
| menus | 菜单集合 | 管理端可读写 |
| orders | 订单集合 | 创建者可读写 |
| users | 用户集合 | 创建者可读写 |
| evaluations | 评价集合 | 创建者可读写 |
| configs | 配置集合 | 仅管理端可写 |

### 2.2 menus (菜单集合)
```javascript
{
  _id: "自动生成",
  date: "2026-04-03",           // 日期，格式 YYYY-MM-DD
  meals: [                       // 餐次列表
    {
      type: "早餐",              // 餐次类型：早餐/午餐/晚餐
      dishes: [                  // 菜品列表
        { name: "豆浆" },
        { name: "油条" }
      ]
    },
    {
      type: "午餐",
      dishes: [
        { name: "红烧肉" },
        { name: "青菜" }
      ]
    }
  ],
  createTime: Date,              // 创建时间
  updateTime: Date              // 更新时间
}
```

### 2.3 orders (订单集合)
```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",        // 用户唯一标识
  date: "2026-04-03",           // 订餐日期
  mealType: "早餐",             // 餐次类型：早餐/午餐/晚餐
  dishes: [                      // 菜品列表
    { name: "豆浆" },
    { name: "油条" }
  ],
  status: "pending",            // 订单状态
  createTime: Date,
  updateTime: Date
}
```

**status 状态说明**：
- `pending`：待用餐（默认）
- `completed`：已完成（自动计算，详见订单状态逻辑）
- `cancelled`：已取消

### 2.4 users (用户集合)
```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  userInfo: {                    // 用户信息
    nickName: "张三",
    avatarUrl: "https://..."
  },
  isAdmin: false,               // 是否管理员
  createTime: Date,
  updateTime: Date
}
```

### 2.5 evaluations (评价集合)
```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  orderId: "订单id",
  rating: 5,                    // 评分 1-5
  comment: "很好吃",            // 评价内容
  createTime: Date
}
```

### 2.6 configs (配置集合)
```javascript
{
  _id: "自动生成",
  key: "order_deadline",        // 配置键名
  breakfast_deadline: "08:00",  // 早餐截止时间
  lunch_deadline: "12:00",     // 午餐截止时间
  dinner_deadline: "17:00",    // 晚餐截止时间
  updateTime: Date
}
```

---

## 三、核心功能逻辑

### 3.1 openid 获取流程

**重要**：openid 是微信用户在小程序中的唯一标识，用于识别用户身份。

```
app.js onLaunch()
    ↓
调用云函数 quickstartFunctions (type: getOpenId)
    ↓
获取 openid，存储到 app.globalData.openid
    ↓
检查管理员身份 (app.checkIsAdmin)
```

**前端使用 openid**：
```javascript
const app = getApp();
// 方式1：通过 app.globalData 获取
const openid = app.globalData.openid;

// 方式2：确保 openid 已获取
if (app.globalData.openid) {
  // 可以安全使用 openid
}
```

**注意事项**：
- 小程序前端直接操作数据库时，不能使用 `'{openid}'` 占位符
- 占位符 `'{openid}'` 仅在**云函数中**有效
- 前端必须先获取真实 openid，再进行数据库查询

### 3.2 订餐页面 (order)

#### 核心数据结构
```javascript
data: {
  selectedDate: '2026-04-03',        // 当前选择的日期
  selectedDateStr: '今天 4月3日',     // 中文显示
  menuData: { ... },                   // 菜单数据
  selectedDishes: {                   // 用户已选的菜品
    '早餐': ['豆浆', '油条'],
    '午餐': [],
    '晚餐': []
  },
  existingOrders: {                   // 该用户已有的订单
    '早餐': { _id: 'xxx', status: 'pending' },
    '午餐': null,
    '晚餐': null
  },
  disabledMeals: {                    // 各餐次是否已截止
    '早餐': false,
    '午餐': false,
    '晚餐': false
  },
  breakfastDeadline: '08:00',         // 早餐截止时间
  lunchDeadline: '12:00',             // 午餐截止时间
  dinnerDeadline: '17:00'             // 晚餐截止时间
}
```

#### 页面加载流程
```
onLoad()
    ↓
initDate() - 初始化当前日期
    ↓
loadSettings() - 从 configs 集合加载截止时间配置
    ↓
waitForOpenid() - 等待 openid 获取
    ↓
loadMenu() - 加载菜单和用户订单
```

#### 加载菜单和订单 (loadMenu)
```javascript
loadMenu() {
  // 1. 查询菜单
  db.collection('menus').where({ date: selectedDate }).get()

  // 2. 分别查询该用户每餐的订单
  const promises = ['早餐', '午餐', '晚餐'].map(mealType => {
    return db.collection('orders').where({
      date: selectedDate,
      mealType: mealType,
      _openid: openid  // 使用真实 openid
    }).get()
  })

  // 3. 处理查询结果
  // - validOrders: status !== 'cancelled' 的订单，用于显示"已订"
  // - cancelledOrders: 已取消的订单，保留订单ID用于重新激活
  // - selectedDishes: 从有效订单中提取已选菜品
}
```

#### 菜品选择 (toggleDish)
```javascript
toggleDish(e) {
  const { mealType, dishName } = e.currentTarget.dataset

  // 1. 检查是否已截止
  if (disabledMeals[mealType]) {
    return // 不可操作
  }

  // 2. 切换选中状态
  const dishes = selectedDishes[mealType]
  const index = dishes.indexOf(dishName)

  if (index > -1) {
    dishes.splice(index, 1)  // 取消选中
  } else {
    dishes.push(dishName)    // 添加选中
  }

  // 3. 更新数据
  setData({ selectedDishes })
}
```

#### 订单提交 (submitOrder)
```javascript
doSubmitOrder(mealType, dishes) {
  const orderData = {
    date: selectedDate,
    mealType: mealType,
    dishes: dishes.map(name => ({ name })),
    status: 'pending',
    createTime: new Date(),
    updateTime: new Date()
  }

  // existingOrders[mealType] 可能有两种情况：
  // 1. validOrders[0]: 有效订单 → update
  // 2. cancelledOrders[0]: 已取消订单 → update (重新激活)

  if (existingOrders[mealType]) {
    // 更新已有订单
    db.collection('orders').doc(existingOrder._id).update({ data: orderData })
  } else {
    // 新增订单
    db.collection('orders').add({ data: orderData })
  }
}
```

#### 菜品选中状态判断 (isDishSelected)
由于 WXML 中直接使用 `selectedDishes[item.type].indexOf(dish.name)` 有时无法正确响应数据变化，封装为方法：
```javascript
isDishSelected(mealType, dishName) {
  const dishes = this.data.selectedDishes[mealType] || []
  return dishes.indexOf(dishName) !== -1
}
```

### 3.3 订单状态逻辑

#### 自动状态计算 (calculateStatus)
```javascript
calculateStatus(order) {
  const deadlineMap = {
    '早餐': '09:00',   // 早餐9点后变已完成
    '午餐': '13:00',   // 午餐13点后变已完成
    '晚餐': '19:00'    // 晚餐19点后变已完成
  }

  // 1. 数据库状态优先
  if (order.status === 'cancelled') return 'cancelled'
  if (order.status === 'completed') return 'completed'

  // 2. 自动计算状态
  const deadline = deadlineMap[order.mealType]
  const deadlineTime = new Date(order.date + ' ' + deadline)

  if (new Date() > deadlineTime) {
    return 'completed'
  }
  return 'pending'
}
```

#### 取消预约 (cancelOrder)
- 状态改为 `cancelled`
- 数据库中保留记录
- 订餐页面不会显示"已订"
- 管理端订餐名单不会显示

#### 是否可取消 (canCancel)
```javascript
canCancel(order) {
  // 已取消或已完成不可取消
  if (order.status === 'cancelled' || order.status === 'completed') {
    return false
  }

  // 超过截止时间不可取消
  const deadline = deadlineMap[order.mealType]
  const deadlineTime = new Date(order.date + ' ' + deadline)
  return new Date() < deadlineTime
}
```

### 3.4 菜单管理页面 (menu)

#### 过去日期限制
```javascript
prevDay() {
  const current = new Date(selectedDate)
  current.setDate(current.getDate() - 1)

  const today = new Date()
  today.setHours(0, 0, 0, 0)

  // 如果选择的日期在今天之前，标记为过去日期
  const isPast = current < today

  this.setData({ isPast })
}
```

- `isPast = true` 时：不能添加菜品、不能删除菜品
- `isPast = false` 时：可以正常操作

### 3.5 订餐名单页面 (orderList)

查询时过滤已取消订单：
```javascript
db.collection('orders').where({
  date: selectedDate,
  status: db.command.neq('cancelled')  // 排除已取消
}).get()
```

### 3.6 订阅消息

#### 配置
在 `miniprogram/config.js` 中配置：
```javascript
module.exports = {
  subscribeMessageTemplateId: '您的订阅消息模板ID'
}
```

#### 发送流程
```
用户点击"订餐"
    ↓
requestSubscribeMessage() - 请求用户授权
    ↓
用户同意/拒绝
    ↓
doSubmitOrder() - 提交订单
    ↓
sendSubscribeMessage() - 调用云函数发送消息
```

#### 云函数 (sendSubscribeMessage)
```javascript
exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  // event 包含: templateId, data, page

  await cloud.openapi.subscribeMessage.send({
    touser: wxContext.OPENID,  // 自动获取用户openid
    templateId: templateId,
    data: data
  })
}
```

---

## 四、页面路由

### 4.1 tabBar 页面（底部导航）
| 页面 | 路径 | 说明 |
|------|------|------|
| 首页 | /pages/index/index | 显示菜单 |
| 订餐 | /pages/order/order | 在线订餐 |
| 订单 | /pages/history/history | 历史订单 |
| 我的 | /pages/profile/profile | 个人中心 |

### 4.2 管理端页面
| 页面 | 路径 | 说明 |
|------|------|------|
| 管理后台 | /pages/admin/admin | 入口页面 |
| 菜单管理 | /pages/menu/menu | 设置每日菜单 |
| 订餐名单 | /pages/orderList/orderList | 查看订餐用户 |
| 数据统计 | /pages/statistics/statistics | 订餐数据 |
| 订餐设置 | /pages/settings/settings | 截止时间配置 |

### 4.3 其他页面
| 页面 | 路径 | 说明 |
|------|------|------|
| 评价 | /pages/evaluate/evaluate | 评价菜品 |

---

## 五、初始化与部署

### 5.1 初始化数据库
1. 上传并部署 `cloudfunctions/quickstartFunctions` 云函数
2. 在小程序首页点击"初始化数据库"按钮
3. 云函数会自动创建 5 个集合并插入默认配置

### 5.2 设置管理员
1. 在云开发控制台找到 `users` 集合
2. 找到目标用户的记录
3. 将 `isAdmin` 字段改为 `true`

### 5.3 配置订阅消息
1. 在微信公众平台申请订阅消息模板
2. 将模板ID填入 `miniprogram/config.js`

### 5.4 云函数配置权限
`sendSubscribeMessage` 云函数需要配置权限：
```json
{
  "permissions": {
    "subscribeMessage": {
      "send": true
    }
  }
}
```

---

## 六、注意事项

### 6.1 openid 使用
- 前端直接操作数据库时，**必须使用真实 openid**
- 不能使用 `'{openid}'` 占位符（该占位符仅在云函数中有效）

### 6.2 日期格式化
- 数据库存储格式：`YYYY-MM-DD`
- 页面显示格式：`今天 4月3日`、`明天 4月4日`、`4月5日`

### 6.3 订单设计
- 订单以**顿**为单位，不是以天为单位
- 每顿饭独立一条订单记录
- 取消预约只改变 status，不删除记录

### 6.4 状态自动计算
- 订单状态由前端根据时间自动计算显示
- 数据库中 status 字段只有 `pending` 和 `cancelled` 两种变化
- `completed` 状态是前端计算出来的，不会写入数据库

---

## 七、待解决问题

### 7.1 菜品选中高亮（已知问题）
**现象**：点击菜品后，`selectedDishes` 数据已更新，但菜品高亮不显示。

**当前状态**：问题仍在排查中，WXML 中使用 `isDishSelected()` 方法判断选中状态。

**可能原因**：
1. WXML 表达式 `indexOf()` 在某些情况下不能正确响应数据变化
2. 数据结构深拷贝问题
3. 微信小程序视图层渲染问题

**临时方案**：使用 `isDishSelected()` 方法替代直接表达式计算。
