# 食堂订餐小程序 - 项目开发文档

---

## 一、项目概述

### 1.1 项目简介

基于微信小程序的食堂订餐系统，支持菜单管理、在线订餐、订单管理、菜品评价、意见建议、用户身份验证等功能。

**核心特性**：
- 统一订餐页面：整合首页和订餐页为一体
- 身份验证：用户需验证身份后才能使用
- 多角色支持：普通工作人员、管理员、厨房工作人员
- 菜品统计：订餐名单显示每道菜需要备多少份
- 手机号标识：用户识别使用 phone 字段
- 意见建议：用户可提交反馈，管理端可查看
- 数据导出：支持导出订餐数据为Excel/CSV格式
- 菜品管理：独立的菜品库管理系统
- 定时提醒：自动发送订餐提醒和用餐提醒

### 1.2 技术栈

| 类别 | 技术 |
|------|------|
| 前端 | 微信小程序原生开发 (WXML + WXSS + JS) |
| 后端 | 微信云开发 (云函数、云数据库) |
| 订阅消息 | 微信订阅消息 API |
| 身份验证 | 微信手机号授权 + 单位人员列表 |

### 1.3 项目结构

```
miniprogram-1/
├── miniprogram/                 # 小程序前端代码
│   ├── app.js                  # 应用入口（初始化、云函数调用）
│   ├── app.json                # 应用配置
│   ├── app.wxss                # 全局样式
│   ├── config.js               # 配置文件（订阅消息模板ID）
│   ├── envList.js              # 环境变量列表
│   ├── utils/                  # 工具类目录
│   │   ├── auth.js             # 权限检查工具类
│   │   ├── dateUtil.js         # 日期工具类
│   │   ├── httpUtil.js         # HTTP请求工具类
│   │   └── initUtil.js         # 异步初始化工具类
│   ├── pages/                  # 页面目录
│   │   ├── auth/               # 身份验证页
│   │   ├── index/              # 首页（保留但已不使用）
│   │   ├── order/              # 订餐页（统一页面）
│   │   ├── history/            # 历史订单
│   │   ├── profile/            # 个人中心
│   │   ├── admin/              # 管理后台入口
│   │   ├── menu/               # 菜单管理
│   │   ├── statistics/         # 菜品评价
│   │   ├── evaluate/           # 评价页面
│   │   ├── orderList/          # 订餐名单
│   │   ├── settings/           # 订餐设置
│   │   ├── userManage/         # 用户管理
│   │   ├── dishManage/         # 菜品管理
│   │   ├── dataExport/         # 数据导出
│   │   ├── reminderSettings/   # 提醒设置
│   │   ├── orderStats/         # 订餐统计
│   │   ├── help/               # 使用帮助
│   │   ├── feedback/           # 意见建议提交
│   │   └── feedbackList/       # 意见建议列表
├── cloudfunctions/              # 云函数目录
│   ├── userFunctions/           # 用户相关云函数
│   ├── orderFunctions/          # 订单相关云函数
│   ├── menuFunctions/           # 菜单管理云函数
│   ├── configFunctions/         # 配置管理云函数
│   ├── dishFunctions/           # 菜品管理云函数
│   ├── exportFunctions/         # 数据导出云函数
│   ├── mealReminder/            # 用餐提醒定时触发云函数
│   ├── orderReminder/           # 订餐提醒定时触发云函数
│   └── sendSubscribeMessage/    # 订阅消息发送云函数
├── cloudbaserc.json            # 云开发配置
├── package.json                # 项目依赖
└── 项目开发文档.md              # 本文档
```

---

## 二、数据库设计

### 2.1 集合列表

| 集合名 | 说明 | 权限建议 |
|--------|------|----------|
| users | 用户集合（已验证用户） | 创建者可读写 |
| staffs | 单位人员列表 | 云函数全权处理写入 |
| menus | 菜单集合 | 管理端可读写 |
| orders | 订单集合 | 创建者可读写 |
| evaluations | 评价集合 | 创建者可读写 |
| configs | 配置集合 | 仅管理端可写 |
| feedbacks | 意见建议集合 | 创建者可读写 |
| dishes | 菜品库集合 | 管理端可读写 |

### 2.2 users（用户集合 - 已验证用户）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 手机号（唯一标识）
  name: "张三",                    // 员工姓名
  isVerified: true,               // 是否已通过身份校验
  role: "staff",                  // 角色：staff/admin/kitchen
  userInfo: {},                   // 微信用户信息
  subscribeOrderReminder: true,   // 订餐提醒开关
  subscribeMealReminder: true,    // 用餐提醒开关
  createTime: Date,
  updateTime: Date
}
```

### 2.3 staffs（单位人员列表）

```javascript
{
  _id: "自动生成",
  phone: "13800138000",         // 手机号（唯一标识）
  name: "张三",                  // 姓名
  role: "staff",                // 角色：staff/admin/kitchen
  status: "active",             // 状态：active/inactive
  createTime: Date,
  updateTime: Date
}
```

### 2.4 menus（菜单集合）

```javascript
{
  _id: "自动生成",
  date: "2026-04-03",           // 日期，格式 YYYY-MM-DD
  meals: [                       // 餐次列表
    {
      type: "早餐",              // 餐次类型：早餐/午餐/晚餐
      dishes: [                  // 菜品列表
        { name: "豆浆" },
        { name: "油条" }
      ]
    },
    {
      type: "午餐",
      dishes: [
        { name: "红烧肉" },
        { name: "青菜" }
      ]
    }
  ],
  createTime: Date,
  updateTime: Date
}
```

### 2.5 orders（订单集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",        // 用户唯一标识
  phone: "13800138000",          // 手机号（用于用户识别）
  date: "2026-04-03",           // 订餐日期
  mealType: "早餐",              // 餐次类型：早餐/午餐/晚餐
  dishes: [                      // 菜品列表
    { name: "豆浆" },
    { name: "油条" }
  ],
  status: "pending",              // 订单状态
  evaluated: false,               // 是否已评价
  createTime: Date,
  updateTime: Date
}
```

**status 状态说明**：
- `pending`：待用餐（默认）
- `completed`：已完成（自动计算，详见订单状态逻辑）
- `cancelled`：已取消

### 2.6 evaluations（评价集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 评价人手机号
  orderId: "订单id",
  date: "2026-04-03",             // 评价日期
  mealType: "早餐",               // 餐次类型
  dishName: "豆浆",               // 菜品名称
  rating: 5,                      // 评分 1-5
  comment: "很好吃",              // 评价内容
  createTime: Date
}
```

**评价唯一性**：一条评价由 `date + mealType + dishName + phone` 唯一确定。

### 2.7 configs（配置集合）

```javascript
{
  _id: "自动生成",
  key: "order_deadline",        // 配置键名
  breakfast_deadline: "08:00",   // 早餐截止时间
  lunch_deadline: "12:00",      // 午餐截止时间
  dinner_deadline: "17:00",     // 晚餐截止时间
  breakfast_meal_start: "08:00", // 早餐开饭时间
  lunch_meal_start: "12:00",     // 午餐开饭时间
  dinner_meal_start: "17:30",   // 晚餐开饭时间
  updateTime: Date
}
```

### 2.8 feedbacks（意见建议集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 提交人手机号
  name: "张三",                   // 提交人姓名
  content: "建议增加更多素菜...", // 建议内容
  createTime: Date
}
```

### 2.9 dishes（菜品库集合）

```javascript
{
  _id: "自动生成",
  name: "红烧肉",                 // 菜品名称
  createTime: Date
}
```

**说明**：菜品库独立于菜单，用于菜单设置时快速选择菜品。

---

## 三、云函数

### 3.1 userFunctions（用户相关云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| getOpenId | 获取用户openid | - | openid, appid, unionid |
| getPhoneNumber | 获取手机号 | code | phoneNumber |
| verifyAndBindPhone | 验证并绑定手机号 | phone | success, data |
| updateAvatar | 更新用户头像 | avatarUrl | success/error |
| getStaffList | 获取员工列表 | - | 员工列表 |
| addStaff | 添加员工 | data | success/error |
| updateStaff | 更新员工 | id, data | success/error |
| deleteStaff | 删除员工 | id | success/error |
| submitFeedback | 提交意见建议 | content | success/error |
| getFeedbackList | 获取意见建议列表 | page, pageSize | list, total, hasMore |

### 3.2 orderFunctions（订单相关云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| getMenu | 获取菜单 | date | 菜单数据 |
| submitOrder | 提交订单 | date, mealType, dishes, phone, existingOrderId | success, orderId |
| getOrderList | 获取订单列表 | date | 订单列表 |
| getStatistics | 获取统计数据 | date | totalOrders, dishStats |
| submitEvaluation | 提交评价 | orderId, evaluations | success |
| submitSingleEvaluation | 提交单个菜品评价 | evaluation | success |
| getEvaluationStatistics | 获取评价统计 | date | 评价统计数据 |
| cancelOrder | 取消订单 | orderId | success/error |
| getOrderStats | 获取订餐统计 | phone | thirtyDay, total |

### 3.3 menuFunctions（菜单管理云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| saveMenu | 保存菜单 | date, meals | success/error |
| createCanteenCollections | 创建数据库集合 | - | success |

### 3.4 configFunctions（配置管理云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| updateDeadlineConfig | 更新截止时间配置 | data | success/error |
| getDeadlineConfig | 获取截止时间配置 | - | deadline配置 |

### 3.5 dishFunctions（菜品管理云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| getDishList | 获取菜品列表 | - | 菜品列表 |
| addDish | 添加菜品 | name | success/error |
| deleteDish | 删除菜品 | id | success/error |
| batchDeleteDish | 批量删除菜品 | ids | success/error |

### 3.6 exportFunctions（数据导出云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| previewOrderData | 预览订单数据 | startDate, endDate | data, total |
| exportOrderData | 导出订单数据 | startDate, endDate, format | fileID, fileName |

### 3.7 mealReminder（用餐提醒云函数）

**触发方式**：定时触发器

| 触发器名称 | 触发时间 | 功能 |
|-----------|---------|------|
| breakfastReminder | 7:30 | 发送早餐提醒 |
| lunchReminder | 11:30 | 发送午餐提醒 |
| dinnerReminder | 17:00 | 发送晚餐提醒 |

**功能**：查询当日对应餐次的订餐用户，向开启用餐提醒的用户发送订阅消息。

### 3.8 orderReminder（订餐提醒云函数）

**触发方式**：定时触发器

| 触发器名称 | 触发时间 | 功能 |
|-----------|---------|------|
| orderReminder | 20:00 | 提醒用户订明天的餐 |

**功能**：查询明天有菜单但未订餐的用户，向开启订餐提醒的用户发送订阅消息。

### 3.9 sendSubscribeMessage（订阅消息云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| - | 发送订阅消息 | templateId, data, page | success/error |

---

## 四、核心功能逻辑

### 4.1 日期工具类 (dateUtil.js)

#### 功能说明

统一的日期处理工具，提供日期格式化、中文显示、前一天/后一天计算等功能。

#### 核心方法

| 方法 | 说明 |
|------|------|
| `formatDate(date)` | 格式化日期为 YYYY-MM-DD |
| `formatDateChinese(date)` | 格式化日期为中文（今天/明天/昨天 + 月日） |
| `formatDateChineseShort(date)` | 短格式中文日期（月日（今天）） |
| `isPast(dateStr)` | 判断是否是过去的日期 |
| `prevDay(dateStr)` | 计算前一天 |
| `nextDay(dateStr)` | 计算后一天 |

### 4.2 权限检查工具类 (auth.js)

#### 核心方法

| 方法 | 说明 |
|------|------|
| `checkVerified()` | 检查是否已通过身份校验 |
| `getRole()` | 获取当前用户角色 |
| `canAccess(pageType)` | 检查是否有权限访问指定页面 |
| `checkPageAccess(pageType)` | 页面权限检查，自动跳转 |
| `isInitializing()` | 检查是否正在初始化中 |

#### 角色权限定义

| 角色 | tabBar 页面 | 菜单管理 | 订餐名单 | 菜品评价 | 订餐设置 | 意见建议列表 | 用户管理 |
|------|------------|---------|---------|---------|---------|------------|---------|
| staff | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| kitchen | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### 4.3 异步初始化工具类 (initUtil.js)

#### 核心方法

| 方法 | 说明 |
|------|------|
| `waitForAppInit()` | 返回 Promise，等待 `openid` **和** `isVerified` 都存在 |
| `waitForOpenid()` | 返回 Promise，仅等待 `openid` |
| `isInitializing()` | 检查是否正在初始化中 |

### 4.4 HTTP请求工具类 (httpUtil.js)

#### 核心方法

| 方法 | 说明 |
|------|------|
| `request(options)` | 统一HTTP请求封装 |
| `handleError(error)` | 统一错误处理 |

### 4.5 身份验证流程 (auth.js)

```
用户打开小程序
    ↓
app.js onLaunch() 获取 openid
    ↓
检查 isVerified 状态
    ↓
    ├─ 已验证 → 进入订餐页
    │
    └─ 未验证 → 跳转至 /pages/auth/auth
              ↓
         显示身份验证页面
              ↓
         用户输入/获取手机号
              ↓
         查询 staffs 集合验证身份
              ↓
         验证成功 → 更新 users 集合（含name字段）→ 进入订餐页
              ↓
         验证失败 → 提示错误信息
```

### 4.6 订餐页面 (order)

#### 核心功能

- 整合原首页和订餐页功能
- 浏览每日菜单
- 选择菜品
- 提交/修改订单
- 截止时间控制

#### 核心数据结构

```javascript
data: {
  selectedDate: '2026-04-03',        // 当前选择的日期
  selectedDateStr: '今天 4月3日',     // 中文显示
  menuData: { ... },                 // 菜单数据
  orders: {
    '早餐': { orderId: null, dishes: [], status: 'none' },
    '午餐': { orderId: null, dishes: [], status: 'none' },
    '晚餐': { orderId: null, dishes: [], status: 'none' }
  },
  userSelections: {
    '早餐': [],
    '午餐': [],
    '晚餐': []
  },
  deadlines: {
    breakfast: '08:00',
    lunch: '12:00',
    dinner: '17:00'
  },
  disabledMeals: {
    '早餐': false,
    '午餐': false,
    '晚餐': false
  }
}
```

### 4.7 订单状态逻辑

#### 取消预约判断 (canCancel)

根据**截止时间**判断是否可取消：

| 时间段 | 订单状态 | 取消按钮 | 无法取消文案 | 评价按钮 |
|--------|---------|---------|------------|---------|
| 未过截止时间 | 待用餐 | ✅ 显示 | ❌ | ❌ |
| 过了截止，未到开饭 | 待用餐 | ❌ 隐藏 | ✅ 显示 | ❌ |
| 过了开饭时间 | 已完成 | ❌ 隐藏 | ❌ | ✅ 显示 |

### 4.8 订餐名单页面 (orderList)

#### 核心功能

- **外层显示**：每顿饭的订单数和每道菜需要备的份数
- **点击跳转**：点击餐次卡片可查看订单明细
- **明细页面**：只显示菜品列表，不显示用户信息

### 4.9 菜品评价页面 (statistics)

#### 核心功能

- 按日期查看菜品评价统计
- 显示每道菜的平均评分和评价人数
- 支持查看评价文字详情
- 日期切换（前一天/后一天）

### 4.10 意见建议功能

#### 用户端 (feedback)

- 提交意见建议（文字版，最多500字）
- 提交成功后自动返回

#### 管理端 (feedbackList)

- 分页加载建议列表
- 下拉刷新
- 上拉加载更多
- 越晚的建议越靠前

### 4.11 个人中心 (profile)

#### 核心功能

- **用户卡片**：展示头像、员工姓名、加密手机号、角色标签
- **提醒设置**：跳转至提醒设置页面
- **订餐统计**：跳转至订餐统计页面
- **使用帮助**：跳转至帮助页面
- **意见建议**：跳转至意见建议提交页面
- **管理后台**：admin/kitchen 角色可见

### 4.12 订阅消息

#### 配置

在 `miniprogram/config.js` 中配置：

```javascript
module.exports = {
  templates: {
    mealReminder: '用餐提醒模板ID',
    orderReminder: '订餐提醒模板ID'
  },
  getMealReminderTemplateId() {
    return this.templates.mealReminder;
  },
  getOrderReminderTemplateId() {
    return this.templates.orderReminder;
  },
  getSubscribeMessageTemplateId() {
    return this.templates.mealReminder;
  }
}
```

#### 发送流程

```
用户点击"订餐"
    ↓
requestSubscribeMessage() - 请求用户授权
    ↓
用户同意/拒绝
    ↓
doSubmitOrder() - 提交订单
    ↓
sendSubscribeMessage() - 调用云函数发送消息
```

#### 定时提醒

**订餐提醒**（每晚20:00）：
```
定时触发器触发
    ↓
查询明天有菜单但未订餐的用户
    ↓
过滤开启订餐提醒的用户
    ↓
批量发送订阅消息
```

**用餐提醒**（开饭前）：
```
定时触发器触发
    ↓
查询当日对应餐次的订餐用户
    ↓
过滤开启用餐提醒的用户
    ↓
批量发送订阅消息
```

#### 用户偏好设置

用户可在「个人中心 → 提醒设置」中开启/关闭两类提醒。设置保存在 `users` 集合的 `subscribeOrderReminder` 和 `subscribeMealReminder` 字段中。

### 4.13 菜品管理（dishManage）

#### 核心功能

- 维护独立的菜品库
- 添加/删除菜品
- 搜索过滤菜品
- 批量选择和批量删除
- **权限**：admin 和 kitchen 角色均可访问

#### 数据结构

```javascript
data: {
  dishList: [],           // 菜品列表
  filteredDishList: [],   // 过滤后的列表
  searchKeyword: '',      // 搜索关键词
  selectedIds: {},        // 选中的菜品ID
  selectedCount: 0,       // 选中数量
  isAllSelected: false    // 是否全选
}
```

### 4.14 数据导出（dataExport）

#### 核心功能

- 按日期范围导出订餐数据
- 支持Excel和CSV两种格式
- 预览导出数据（前10条）
- 单次最多导出31天数据

#### 导出流程

```
选择日期范围
    ↓
点击预览按钮
    ↓
调用云函数获取前10条数据
    ↓
确认导出
    ↓
调用云函数生成文件
    ↓
上传到云存储
    ↓
下载并打开文件
```

#### 导出内容

| 字段 | 说明 |
|------|------|
| 日期 | 订餐日期 |
| 餐次 | 早餐/午餐/晚餐 |
| 姓名 | 订餐人姓名 |
| 菜品 | 订餐菜品列表 |
| 订单状态 | 正常/取消预定 |

---

## 五、页面路由

### 5.1 tabBar 页面（底部导航）

| 页面 | 路径 | 说明 |
|------|------|------|
| 订餐 | /pages/order/order | 统一订餐页面 |
| 订单 | /pages/history/history | 历史订单 |
| 我的 | /pages/profile/profile | 个人中心 |

### 5.2 身份验证页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 身份验证 | /pages/auth/auth | 首次使用或未验证用户 |

### 5.3 管理端页面

| 页面 | 路径 | 说明 | 权限 |
|------|------|------|------|
| 管理后台 | /pages/admin/admin | 入口页面 | admin/kitchen |
| 菜单管理 | /pages/menu/menu | 设置每日菜单 | admin/kitchen |
| 订餐名单 | /pages/orderList/orderList | 查看订餐用户 | admin/kitchen |
| 菜品评价 | /pages/statistics/statistics | 菜品评价统计 | admin/kitchen |
| 订餐设置 | /pages/settings/settings | 截止时间配置 | admin/kitchen |
| 意见建议 | /pages/feedbackList/feedbackList | 用户反馈列表 | admin/kitchen |
| 菜品管理 | /pages/dishManage/dishManage | 菜品库管理 | admin |
| 数据导出 | /pages/dataExport/dataExport | 导出订餐数据 | admin |
| 用户管理 | /pages/userManage/userManage | 单位人员管理 | admin |

### 5.4 功能页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 评价 | /pages/evaluate/evaluate | 评价菜品 |
| 提醒设置 | /pages/reminderSettings/reminderSettings | 提醒开关 |
| 订餐统计 | /pages/orderStats/orderStats | 个人订餐统计 |
| 使用帮助 | /pages/help/help | 功能说明 |
| 意见建议 | /pages/feedback/feedback | 提交意见建议 |

---

## 六、初始化与部署

### 6.1 初始化数据库

1. 上传并部署所有云函数
2. 在小程序首页点击"初始化数据库"按钮
3. 云函数会自动创建集合并插入默认配置

### 6.2 设置管理员

1. 在云开发控制台找到 `staffs` 集合
2. 添加一条新记录：

```javascript
{
  phone: "您的手机号",
  name: "管理员",
  role: "admin",
  status: "active"
}
```

3. 使用该手机号登录小程序完成身份验证

### 6.3 配置订阅消息

1. 在微信公众平台申请订阅消息模板
2. 将模板ID填入 `miniprogram/config.js`

### 6.4 部署云函数

需要部署的云函数列表：
- userFunctions
- orderFunctions
- menuFunctions
- configFunctions
- dishFunctions
- exportFunctions
- mealReminder
- orderReminder
- sendSubscribeMessage

### 6.5 配置定时触发器

在云开发控制台为以下云函数配置定时触发器：

#### mealReminder（用餐提醒）

| 触发器名称 | Cron表达式 | 说明 |
|-----------|-----------|------|
| breakfastReminder | 0 30 7 * * * * | 每天7:30触发 |
| lunchReminder | 0 30 11 * * * * | 每天11:30触发 |
| dinnerReminder | 0 0 17 * * * * | 每天17:00触发 |

#### orderReminder（订餐提醒）

| 触发器名称 | Cron表达式 | 说明 |
|-----------|-----------|------|
| orderReminder | 0 0 20 * * * * | 每天20:00触发 |

---

## 七、注意事项

### 7.1 手机号标识

- 订单查询使用 `phone` 字段作为用户标识
- 前端从 `app.globalData.phone` 获取
- 取消订单云函数使用 `phone` 验证订单归属

### 7.2 openid 使用

- openid 仍记录在订单中用于微信消息发送
- 前端直接操作数据库时，不能使用 `'{openid}'` 占位符
- 使用 `initUtil.waitForAppInit()` 确保 openid 已获取

### 7.3 日期格式化

- 数据库存储格式：`YYYY-MM-DD`
- 页面显示格式：`今天 4月3日`、`明天 4月4日`、`4月5日`
- 所有日期逻辑使用 `dateUtil.js` 工具类

### 7.4 订单设计

- 订单以**顿**为单位，不是以天为单位
- 每顿饭独立一条订单记录
- 取消预约只改变 status，不删除记录
- 订单按 phone 过滤，每个用户只能看到自己的订单

### 7.5 状态自动计算

- 订单状态由前端根据时间自动计算显示
- 数据库中 status 字段只有 `pending` 和 `cancelled` 两种变化
- `completed` 状态是前端计算出来的，不会写入数据库

---

## 八、数据操作规范

### 8.1 云函数封装

为保证数据安全和业务逻辑统一，建议将所有数据操作收敛到云函数中。

#### 可保持前端直查的操作

| 集合 | 操作 | 原因 |
|------|------|------|
| menus | 读取菜单 | 菜单是公开数据 |
| configs | 读取配置 | 配置是公开信息 |
| evaluations | 提交评价 | 评价功能保持简单 |

#### 需使用云函数操作

| 集合 | 操作 | 原因 |
|------|------|------|
| orders | 查询/提交/取消 | 需要 phone 过滤 + 归属验证 |
| users | 查询/更新用户信息 | 需要身份验证 |
| menus | 保存菜单 | 需要 admin/kitchen 权限 |
| configs | 保存配置 | 需要 admin/kitchen 权限 |
| feedbacks | 查询列表 | 需要 admin/kitchen 权限 |

---

## 九、版本更新记录

### v1.6 版本

#### 功能更新

- **数据导出**：支持按日期范围导出订餐数据（Excel/CSV格式）
- **菜品管理**：独立的菜品库管理，支持增删改查和批量操作
- **定时提醒**：
  - 订餐提醒：每晚20:00自动提醒未订餐用户
  - 用餐提醒：开饭前自动提醒已订餐用户
- **多模板订阅消息**：支持订餐提醒和用餐提醒两种模板

#### 数据库更新

- 新增 `dishes` 集合（菜品库）

#### 云函数更新

- 新增 `dishFunctions` 云函数模块（菜品管理）
- 新增 `exportFunctions` 云函数模块（数据导出）
- 新增 `mealReminder` 云函数（用餐提醒定时触发）
- 新增 `orderReminder` 云函数（订餐提醒定时触发）

#### 页面更新

- 新增 `dishManage` 页面（菜品管理）
- 新增 `dataExport` 页面（数据导出）

### v1.5 版本

#### 功能更新

- **意见建议功能**：用户可提交意见建议，管理员和厨房可查看反馈列表
- **菜品评价页面重构**：移除订餐统计，只显示菜品评价数据
- **管理后台菜单调整**：数据分析改名为菜品评价
- **云函数重构**：拆分为多个独立云函数模块

#### 数据库更新

- 新增 `feedbacks` 集合

#### 云函数更新

- 新增 `submitFeedback` 云函数
- 新增 `getFeedbackList` 云函数
- 新增 `configFunctions` 云函数模块
- 新增 `menuFunctions` 云函数模块
- 新增 `userFunctions` 云函数模块

#### 页面更新

- 新增 `feedback` 页面
- 新增 `feedbackList` 页面

### v1.4 版本

#### 功能更新

- **评价系统重构**：每道菜单独提交评价，评价由日期+餐次+菜名+手机号唯一确定
- **数据分析升级**：按天展示每道菜平均评分，支持查看文字评价详情
- **用户管理优化**：搜索过滤、批量选择、批量离职

#### 数据库更新

- `evaluations` 集合新增 `date`、`phone` 字段

#### 云函数更新

- `submitEvaluation` 新增 date、phone 字段存储
- 新增 `getEvaluationStatistics` 云函数

### v1.3 版本

- **用户卡片**：个人中心展示头像、姓名、加密手机号、角色标签
- **提醒设置**：新增 reminderSettings 页面，支持订餐/用餐提醒开关
- **订餐统计**：新增 orderStats 页面，展示近30天和总计订餐次数
- **使用帮助**：新增 help 页面，按角色展示功能说明
- **手机号标识**：订单查询从 _openid 改为 phone

### v1.2 版本

- 新增身份验证功能
- 新增用户管理功能
- 新增多角色支持
- 合并首页和订餐页为统一订餐页面

### v1.1 版本

- 订单以顿为单位
- 支持取消预约
- 新增日期工具类

### v1.0 版本

- 基础功能实现
