# 食堂订餐小程序 - 项目开发文档

---

## 一、项目概述

### 1.1 项目简介

基于微信小程序的食堂订餐系统，支持菜单管理、在线订餐、订单管理、数据统计、用户身份验证等功能。

**核心特性**：
- 统一订餐页面：整合首页和订餐页为一体
- 身份验证：用户需验证身份后才能使用
- 多角色支持：普通工作人员、管理员、厨房工作人员
- 菜品统计：订餐名单显示每道菜需要备多少份

### 1.2 技术栈

- **前端**：微信小程序原生开发 (WXML + WXSS + JS)
- **后端**：微信云开发 (云函数、云数据库、云存储)
- **订阅消息**：微信订阅消息
- **身份验证**：微信手机号授权 + 单位人员列表

### 1.3 项目结构

```
miniprogram-1/
├── miniprogram/                 # 小程序前端代码
│   ├── app.js                  # 应用入口（身份验证逻辑）
│   ├── app.json                # 应用配置
│   ├── app.wxss                # 全局样式
│   ├── config.js               # 配置文件（订阅消息模板ID）
│   ├── utils/                  # 工具类目录
│   │   ├── auth.js             # 权限检查工具类
│   │   ├── dateUtil.js         # 日期工具类
│   │   └── initUtil.js         # 异步初始化工具类
│   └── pages/                  # 页面目录
│       ├── auth/               # 身份验证页（新增）
│       ├── order/              # 订餐页（合并后的统一页面）
│       ├── history/            # 历史订单
│       ├── profile/            # 个人中心
│       ├── admin/              # 管理后台入口
│       ├── menu/               # 菜单管理
│       ├── statistics/         # 数据统计
│       ├── evaluate/           # 评价页面
│       ├── orderList/          # 订餐名单
│       ├── settings/           # 订餐设置
│       └── userManage/         # 用户管理（新增）
├── cloudfunctions/              # 云函数目录
│   ├── quickstartFunctions/     # 通用云函数
│   └── sendSubscribeMessage/   # 订阅消息发送云函数
└── 项目开发文档.md              # 本文档
```

---

## 二、数据库设计

### 2.1 集合列表

| 集合名 | 说明 | 权限建议 |
|--------|------|----------|
| menus | 菜单集合 | 管理端可读写 |
| orders | 订单集合 | 创建者可读写 |
| users | 用户集合（已验证用户） | 创建者可读写 |
| staffs | 单位人员列表 | 云函数全权处理写入 |
| evaluations | 评价集合 | 创建者可读写 |
| configs | 配置集合 | 仅管理端可写 |

### 2.2 menus (菜单集合)

```javascript
{
  _id: "自动生成",
  date: "2026-04-03",           // 日期，格式 YYYY-MM-DD
  meals: [                       // 餐次列表
    {
      type: "早餐",              // 餐次类型：早餐/午餐/晚餐
      dishes: [                  // 菜品列表
        { name: "豆浆" },
        { name: "油条" }
      ]
    },
    {
      type: "午餐",
      dishes: [
        { name: "红烧肉" },
        { name: "青菜" }
      ]
    }
  ],
  createTime: Date,              // 创建时间
  updateTime: Date              // 更新时间
}
```

### 2.3 orders (订单集合)

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",        // 用户唯一标识
  date: "2026-04-03",           // 订餐日期
  mealType: "早餐",             // 餐次类型：早餐/午餐/晚餐
  dishes: [                      // 菜品列表
    { name: "豆浆" },
    { name: "油条" }
  ],
  status: "pending",            // 订单状态
  createTime: Date,
  updateTime: Date
}
```

**status 状态说明**：
- `pending`：待用餐（默认）
- `completed`：已完成（自动计算，详见订单状态逻辑）
- `cancelled`：已取消

### 2.4 users (用户集合 - 已验证用户)

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",        // 用户唯一标识
  phone: "13800138000",         // 绑定的手机号
  isVerified: true,             // 是否已通过身份校验
  role: "staff",                // 角色：staff/admin/kitchen
  createTime: Date,
  updateTime: Date
}
```

### 2.5 staffs (单位人员列表)

```javascript
{
  _id: "自动生成",
  phone: "13800138000",         // 手机号（唯一标识）
  name: "张三",                  // 姓名
  role: "staff",                // 身份角色
  // role 可选值：
  // - "staff": 普通工作人员
  // - "admin": 管理员
  // - "kitchen": 厨房工作人员
  status: "active",             // 状态：active/inactive
  createTime: Date,
  updateTime: Date
}
```

### 2.6 evaluations (评价集合)

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  orderId: "订单id",
  rating: 5,                    // 评分 1-5
  comment: "很好吃",            // 评价内容
  createTime: Date
}
```

### 2.7 configs (配置集合)

```javascript
{
  _id: "自动生成",
  key: "order_deadline",        // 配置键名
  breakfast_deadline: "08:00",  // 早餐截止时间
  lunch_deadline: "12:00",     // 午餐截止时间
  dinner_deadline: "17:00",    // 晚餐截止时间
  updateTime: Date
}
```

---

## 三、核心功能逻辑

### 3.1 日期工具类 (dateUtil.js)

#### 功能说明

统一的日期处理工具，提供日期格式化、中文显示、前一天/后一天计算等功能。

#### 核心方法

| 方法 | 说明 |
|------|------|
| `formatDate(date)` | 格式化日期为 YYYY-MM-DD |
| `formatDateChinese(date)` | 格式化日期为中文（今天/明天/昨天 + 月日） |
| `formatDateChineseShort(date)` | 短格式中文日期（月日（今天）） |
| `isPast(dateStr)` | 判断是否是过去的日期 |
| `prevDay(dateStr)` | 计算前一天 |
| `nextDay(dateStr)` | 计算后一天 |

#### 使用示例

```javascript
const dateUtil = require('../../utils/dateUtil.js');

// 初始化日期
const now = new Date();
this.setData({
  selectedDate: dateUtil.formatDate(now),
  selectedDateStr: dateUtil.formatDateChinese(now)
});

// 前一天
const prev = dateUtil.prevDay(this.data.selectedDate);

// 后一天
const next = dateUtil.nextDay(this.data.selectedDate);
```

### 3.2 权限检查工具类 (auth.js)

#### 功能说明

统一的权限检查工具，提供身份验证、角色权限判断、页面访问控制等功能。

#### 核心方法

| 方法 | 说明 |
|------|------|
| `checkVerified()` | 检查是否已通过身份校验 |
| `getRole()` | 获取当前用户角色 |
| `canAccess(pageType)` | 检查是否有权限访问指定页面 |
| `checkPageAccess(pageType)` | 页面权限检查，自动跳转 |

#### 角色权限定义

| 角色 | tabBar 页面 | 菜单管理 | 订餐名单 | 数据统计 | 订餐设置 | 用户管理 |
|------|------------|---------|---------|---------|---------|---------|
| staff（普通工作人员） | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| kitchen（厨房工作人员） | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| admin（管理员） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

#### pageType 参数说明

| pageType | 说明 |
|----------|------|
| `tabbar` | tabBar 页面（所有认证用户可访问） |
| `menu` | 菜单管理页面 |
| `orderList` | 订餐名单页面 |
| `statistics` | 数据统计页面 |
| `settings` | 订餐设置页面 |
| `userManage` | 用户管理页面（仅管理员） |

#### 使用示例

```javascript
const auth = require('../../utils/auth.js');

Page({
  onShow() {
    // 检查页面访问权限
    auth.checkPageAccess('tabbar');
  }
});
```

### 3.3 异步初始化工具类 (initUtil.js)

#### 功能说明

确保页面在 app.js 完成初始化后再执行数据加载，避免渲染层错误。

#### 核心方法

| 方法 | 说明 |
|------|------|
| `waitForAppInit()` | 返回 Promise，等待 app.globalData.openid 存在 |

#### 使用示例

```javascript
const initUtil = require('../../utils/initUtil.js');

Page({
  async onLoad() {
    await initUtil.waitForAppInit();
    this.loadData();
  }
});
```

### 3.4 身份验证流程 (auth.js)

#### 流程说明

```
用户打开小程序
    ↓
app.js onLaunch() 获取 openid
    ↓
检查 isVerified 状态
    ↓
    ├─ 已验证 → 进入首页（订餐页）
    │
    └─ 未验证 → 跳转至 /pages/auth/auth
              ↓
         显示身份验证页面
              ↓
         用户输入/获取手机号
              ↓
         查询 staffs 集合验证身份
              ↓
         验证成功 → 更新 users 集合 → 进入订餐页
              ↓
         验证失败 → 提示错误信息
```

#### 验证方式

1. **微信手机号授权**：使用 `getPhoneNumber` 组件一键获取
2. **手动输入手机号**：用户自行输入手机号

### 3.5 订餐页面 (order)

#### 核心功能

整合了原首页和订餐页的功能，用户可以在同一页面完成：
- 浏览每日菜单
- 选择菜品
- 提交/修改订单

#### 核心数据结构

```javascript
data: {
  selectedDate: '2026-04-03',        // 当前选择的日期
  selectedDateStr: '今天 4月3日',     // 中文显示
  menuData: { ... },                 // 菜单数据
  orders: {
    '早餐': { orderId: null, dishes: [], status: 'none' },
    '午餐': { orderId: null, dishes: [], status: 'none' },
    '晚餐': { orderId: null, dishes: [], status: 'none' }
  },
  userSelections: {
    '早餐': [],
    '午餐': [],
    '晚餐': []
  },
  deadlines: {
    breakfast: '08:00',
    lunch: '12:00',
    dinner: '17:00'
  },
  disabledMeals: {
    '早餐': false,
    '午餐': false,
    '晚餐': false
  }
}
```

#### 页面加载流程

```
onLoad()
    ↓
initDate() - 初始化当前日期
    ↓
loadDeadlines() - 从 configs 集合加载截止时间配置
    ↓
initPage() - 等待 app 初始化完成
    ↓
loadData() - 加载菜单和用户订单
```

#### 加载菜单和订单 (loadData)

- 查询 menus 集合获取当日菜单
- 查询每餐的订单（按 openid 过滤，只显示当前用户的订单）
- 有效订单（status !== 'cancelled'）显示为已订状态
- 已取消或无订单显示为未订餐状态
- 从有效订单中提取已选菜品

#### 菜品选择 (toggleDish)

- 检查是否已截止（disabledMeals）
- 切换选中状态
- 更新 userSelections 数据

#### 订单提交 (submitOrder)

- 检查是否有选择菜品
- 检查是否可订餐
- 新增或更新订单
- 支持订阅消息

### 3.6 订单状态逻辑

#### 自动状态计算 (calculateStatus)

```javascript
calculateStatus(order) {
  const deadlineMap = {
    '早餐': '09:00',   // 早餐9点后变已完成
    '午餐': '13:00',   // 午餐13点后变已完成
    '晚餐': '19:00'    // 晚餐19点后变已完成
  }

  // 1. 数据库状态优先
  if (order.status === 'cancelled') return 'cancelled'
  if (order.status === 'completed') return 'completed'

  // 2. 自动计算状态
  const deadline = deadlineMap[order.mealType]
  const deadlineTime = new Date(order.date + ' ' + deadline)

  if (new Date() > deadlineTime) {
    return 'completed'
  }
  return 'pending'
}
```

#### 取消预约 (cancelOrder)

- 状态改为 `cancelled`
- 数据库中保留记录
- 订餐页面显示为未订餐状态
- 管理端订餐名单不显示

### 3.7 订餐名单页面 (orderList)

#### 核心功能

- **外层显示**：每顿饭的订单数和每道菜需要备的份数
- **点击跳转**：点击餐次卡片可查看订单明细
- **明细页面**：只显示菜品列表，不显示用户信息

#### 数据结构

```javascript
mealStats: {
  '早餐': { dishes: [], orders: [] },
  '午餐': { dishes: [], orders: [] },
  '晚餐': { dishes: [], orders: [] }
}

// dishes 格式：
// [
//   { name: '豆浆', count: 3 },
//   { name: '油条', count: 2 }
// ]
```

#### 查询逻辑

- 过滤已取消订单
- 统计每道菜的份数
- 按餐次分组

### 3.8 用户管理页面 (userManage)

#### 核心功能

- 查看单位人员列表
- 添加新人员
- 编辑人员信息（姓名、角色、状态）
- 删除人员（设为离职状态）

#### 权限控制

- 仅管理员（role === 'admin'）可访问
- 通过云函数全权处理 staffs 集合的增删改查

### 3.9 订阅消息

#### 配置

在 `miniprogram/config.js` 中配置：

```javascript
module.exports = {
  subscribeMessageTemplateId: '您的订阅消息模板ID'
}
```

#### 发送流程

```
用户点击"订餐"
    ↓
requestSubscribeMessage() - 请求用户授权
    ↓
用户同意/拒绝
    ↓
doSubmitOrder() - 提交订单
    ↓
sendSubscribeMessage() - 调用云函数发送消息
```

---

## 四、页面路由

### 4.1 tabBar 页面（底部导航）

| 页面 | 路径 | 说明 |
|------|------|------|
| 订餐 | /pages/order/order | 统一订餐页面 |
| 订单 | /pages/history/history | 历史订单 |
| 我的 | /pages/profile/profile | 个人中心 |

### 4.2 身份验证页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 身份验证 | /pages/auth/auth | 首次使用或未验证用户 |

### 4.3 管理端页面

| 页面 | 路径 | 说明 | 权限 |
|------|------|------|------|
| 管理后台 | /pages/admin/admin | 入口页面 | admin/kitchen |
| 菜单管理 | /pages/menu/menu | 设置每日菜单 | admin/kitchen |
| 订餐名单 | /pages/orderList/orderList | 查看订餐用户 | admin/kitchen |
| 数据统计 | /pages/statistics/statistics | 订餐数据 | admin/kitchen |
| 订餐设置 | /pages/settings/settings | 截止时间配置 | admin/kitchen |
| 用户管理 | /pages/userManage/userManage | 单位人员管理 | admin |

### 4.4 其他页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 评价 | /pages/evaluate/evaluate | 评价菜品 |

---

## 五、初始化与部署

### 5.1 初始化数据库

1. 上传并部署 `cloudfunctions/quickstartFunctions` 云函数
2. 上传并部署 `cloudfunctions/sendSubscribeMessage` 云函数
3. 在小程序首页点击"初始化数据库"按钮
4. 云函数会自动创建集合并插入默认配置

### 5.2 设置管理员

1. 在云开发控制台找到 `staffs` 集合
2. 添加一条新记录：

```javascript
{
  phone: "您的手机号",
  name: "管理员",
  role: "admin",
  status: "active"
}
```

3. 使用该手机号登录小程序完成身份验证

### 5.3 添加单位人员

**方式一：云开发控制台手动添加**

直接在 `staffs` 集合中添加记录。

**方式二：用户管理页面（仅管理员）**

管理员登录后进入"用户管理"页面，可以添加、编辑、删除人员。

### 5.4 配置订阅消息

1. 在微信公众平台申请订阅消息模板
2. 将模板ID填入 `miniprogram/config.js`

### 5.5 云函数配置权限

`sendSubscribeMessage` 云函数需要配置权限：

```json
{
  "permissions": {
    "subscribeMessage": {
      "send": true
    }
  }
}
```

---

## 六、注意事项

### 6.1 openid 使用

- 前端直接操作数据库时，**必须使用真实 openid**
- 不能使用 `'{openid}'` 占位符（该占位符仅在云函数中有效）
- 使用 `initUtil.waitForAppInit()` 确保 openid 已获取

### 6.2 日期格式化

- 数据库存储格式：`YYYY-MM-DD`
- 页面显示格式：`今天 4月3日`、`明天 4月4日`、`4月5日`
- 所有日期逻辑使用 `dateUtil.js` 工具类

### 6.3 订单设计

- 订单以**顿**为单位，不是以天为单位
- 每顿饭独立一条订单记录
- 取消预约只改变 status，不删除记录
- 订单按 openid 过滤，每个用户只能看到自己的订单

### 6.4 状态自动计算

- 订单状态由前端根据时间自动计算显示
- 数据库中 status 字段只有 `pending` 和 `cancelled` 两种变化
- `completed` 状态是前端计算出来的，不会写入数据库

### 6.5 统一页面访问控制

- 所有 tabBar 页面和管理端页面都需要检查身份验证
- 使用 `auth.checkPageAccess(pageType)` 进行统一权限控制
- 未验证用户自动跳转至身份验证页面
- 无权限访问时自动跳转至订餐页面

### 6.6 统一日期组件

- 所有页面使用统一的日期导航组件
- 日期逻辑收敛到 `dateUtil.js` 工具类
- 方便后续统一修改日期相关功能

### 6.7 订餐页面整合

- 原首页和订餐页已合并为统一的"订餐"页面
- 用户可以在同一页面完成浏览菜单和订餐
- tabBar 第一个入口为订餐页面

---

## 七、版本更新记录

### 7.1 v1.0 版本

- 基础功能实现：菜单管理、在线订餐、订单管理、数据统计
- 微信云开发集成
- 订阅消息功能

### 7.1 v1.1 版本

- 重构订餐逻辑：订单以顿为单位
- 优化订单状态管理：支持取消预约
- 新增日期工具类：统一日期处理逻辑
- 优化订餐名单页面：显示菜品统计，支持点击查看明细
- 统一日期选择组件：所有页面使用相同的日期导航
- 修复菜品选中高亮问题

### 7.2 v1.2 版本

- 新增身份验证功能：用户需验证身份后才能使用
- 新增用户管理功能：管理员可管理单位人员
- 新增多角色支持：普通工作人员、管理员、厨房工作人员
- 新增权限检查工具类：统一页面访问控制
- 新增异步初始化工具类：避免渲染层错误
- 合并首页和订餐页为统一的订餐页面
- 优化 tabBar 配置：订餐页面作为第一个入口
- 更新数据库结构：users 集合使用 role 字段，staffs 集合存储单位人员
- 更新云函数：添加手机号获取、staffs 增删改查功能
- 更新订阅消息默认跳转页面

---

## 八、常见问题

### Q1: 如何添加新的管理员？

**答**：在云开发控制台的 `staffs` 集合中添加一条记录，设置 `role` 为 `admin`。

### Q2: 厨房工作人员和管理员有什么区别？

**答**：厨房工作人员可以访问所有管理端功能（菜单管理、订餐名单、数据统计、订餐设置），但不能访问用户管理页面。管理员拥有所有权限。

### Q3: 用户如何完成身份验证？

**答**：用户首次打开小程序时，会显示身份验证页面。用户可以通过微信手机号授权或手动输入手机号进行验证。验证通过后自动进入订餐页面。

### Q4: 为什么用户看不到自己的订单？

**答**：请检查：
1. 用户是否已完成身份验证
2. 用户的 openid 是否正确记录在 orders 集合中
3. 订单的 date 和 mealType 是否正确

### Q5: 如何初始化数据库？

**答**：
1. 部署 `quickstartFunctions` 和 `sendSubscribeMessage` 云函数
2. 在小程序中点击"初始化数据库"按钮
3. 在 `staffs` 集合中添加第一个管理员记录
