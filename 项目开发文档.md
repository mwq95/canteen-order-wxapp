# 食堂订餐小程序 - 开发文档

## 项目概述

这是一个基于微信小程序云开发的食堂订餐系统，用于单位食堂的订餐管理，支持员工订餐、菜单管理、订单统计等功能。

## 技术栈

- **前端**：微信小程序原生开发
- **后端**：微信小程序云开发
- **数据库**：云数据库
- **云函数**：Node.js

## 项目结构

```
miniprogram-1/
├── miniprogram/                 # 小程序前端代码
│   ├── pages/                   # 页面目录
│   │   ├── index/              # 首页 - 菜单展示
│   │   ├── order/              # 订餐页面
│   │   ├── history/            # 历史订单
│   │   ├── profile/            # 个人中心
│   │   ├── admin/              # 管理后台入口
│   │   ├── menu/               # 菜单管理
│   │   ├── statistics/         # 数据分析
│   │   ├── evaluate/           # 菜品评价
│   │   └── orderList/          # 订餐名单
│   ├── images/                 # 图片资源
│   ├── components/             # 组件
│   ├── app.js                  # 小程序入口文件
│   ├── app.json                # 小程序配置
│   ├── app.wxss                # 全局样式
│   └── config.js               # 配置文件（订阅消息模板ID）
├── cloudfunctions/             # 云函数
│   ├── quickstartFunctions/    # 快速开始云函数（含数据库初始化）
│   ├── orderFunctions/         # 订单相关云函数
│   └── sendSubscribeMessage/   # 发送订阅消息云函数
└── project.config.json         # 项目配置
```

## 快速开始

### 1. 环境准备

1. 下载并安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 注册微信小程序并获取 AppID
3. 在微信开发者工具中导入项目

### 2. 开通云开发

1. 在微信开发者工具顶部点击「云开发」按钮
2. 点击「开通」，选择套餐（免费版即可）
3. 创建云开发环境

### 3. 初始化数据库

#### 方法 A：通过小程序自动初始化（推荐）

1. 在微信开发者工具中，右键点击 `cloudfunctions/quickstartFunctions`
2. 选择「上传并部署：云端安装依赖」
3. 等待云函数上传完成
4. 编译运行小程序
5. 首页会显示「首次使用，请初始化数据库」卡片
6. 点击「初始化数据库」按钮
7. 等待提示「初始化成功」

#### 方法 B：在云开发控制台手动创建

1. 点击顶部「云开发」按钮
2. 左侧点击「数据库」
3. 点击「+ 添加集合」
4. 依次创建以下 5 个集合：
   - `users`（用户表）
   - `menus`（菜单表）
   - `orders`（订单表）
   - `evaluations`（评价表）
   - `configs`（配置表，可选）
5. 每个集合创建后，点击「权限设置」，选择「所有用户可读，仅创建者可写」

### 4. 上传所有云函数

依次右键点击以下云函数文件夹，选择「上传并部署：云端安装依赖」：

- `cloudfunctions/quickstartFunctions`
- `cloudfunctions/orderFunctions`
- `cloudfunctions/sendSubscribeMessage`（可选，用于订阅消息）

### 5. 配置订阅消息（可选）

#### 5.1 在微信公众平台申请模板

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入小程序 → 功能 → 订阅消息
3. 点击「选用」或「我的模板」→「添加模板」
4. 选择一个合适的模板（推荐搜索「订餐」、「通知」）
5. 选择关键词（建议：thing1=日期, thing2=菜品, thing3=提示）
6. 提交后获得模板ID

#### 5.2 配置模板ID

打开 `miniprogram/config.js`，将模板ID填入：

```javascript
module.exports = {
  subscribeMessageTemplateId: '您的模板ID',
  
  getSubscribeMessageTemplateId() {
    return this.subscribeMessageTemplateId;
  }
};
```

**注意**：如果不想使用订阅消息，保持默认值即可，程序会自动跳过该功能。

## 数据库设计

### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | String | 记录ID（自动生成） |
| _openid | String | 用户openid（自动生成） |
| userInfo | Object | 用户信息（nickName, avatarUrl等） |
| isAdmin | Boolean | 是否为管理员 |
| createTime | Date | 创建时间 |

### menus（菜单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | String | 记录ID（自动生成） |
| date | String | 日期（YYYY-MM-DD） |
| meals | Array | 餐次数组 |
| meals[].type | String | 餐次类型（早餐/午餐/晚餐） |
| meals[].dishes | Array | 菜品数组 |
| meals[].dishes[].name | String | 菜品名称 |
| meals[].dishes[].price | Number | 菜品价格（可选） |
| createTime | Date | 创建时间 |
| updateTime | Date | 更新时间 |

### orders（订单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | String | 记录ID（自动生成） |
| _openid | String | 用户openid（自动生成） |
| date | String | 订餐日期 |
| dishes | Array | 订菜品数组 |
| dishes[].mealType | String | 餐次类型 |
| dishes[].name | String | 菜品名称 |
| dishes[].price | Number | 菜品价格 |
| totalPrice | String | 总价 |
| status | String | 状态（pending待用餐/completed已完成） |
| evaluated | Boolean | 是否已评价 |
| createTime | Date | 创建时间 |
| updateTime | Date | 更新时间 |

### evaluations（评价表）

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | String | 记录ID（自动生成） |
| _openid | String | 用户openid（自动生成） |
| orderId | String | 订单ID |
| dishName | String | 菜品名称 |
| mealType | String | 餐次类型 |
| rating | Number | 评分（1-5星） |
| comment | String | 评价内容 |
| createTime | Date | 创建时间 |

## 设置管理员

### 方法一：通过云开发控制台（推荐）

1. 让需要设为管理员的用户先登录小程序（「我的」→ 点击登录）
2. 打开云开发控制台 → 数据库 → `users` 集合
3. 找到该用户的记录
4. 点击记录进入详情 → 点击「添加字段」
5. 字段名：`isAdmin`，类型：布尔值，值：`true`
6. 保存后，用户重新进入小程序即可看到「管理后台」入口

### 方法二：直接添加记录

在 `users` 集合中手动添加一条记录，设置 `isAdmin: true`。

## 功能说明

### 普通用户功能

1. **首页**：查看今日和明日菜单
2. **订餐**：选择菜品订餐，支持修改订单
3. **订单**：查看历史订单，删除订单，评价菜品
4. **我的**：登录，管理个人信息

### 管理员功能

1. **菜单管理**：设置每日菜单，添加/删除菜品
2. **订餐名单**：查看每日订餐名单，标记订单完成
3. **数据分析**：查看订餐人数统计、菜品受欢迎程度

## 部署 checklist

- [ ] 开通云开发环境
- [ ] 创建数据库集合（users, menus, orders, evaluations, configs）
- [ ] 设置集合权限为「所有用户可读，仅创建者可写」
- [ ] 上传并部署所有云函数
- [ ] 配置订阅消息模板ID（可选）
- [ ] 设置至少一个管理员账号
- [ ] 在管理后台添加测试菜单
- [ ] 测试订餐功能
- [ ] 测试管理功能

## 常见问题

### Q: 云函数上传失败怎么办？
A: 检查网络连接，确认云开发环境已开通，右键选择「上传并部署：云端安装依赖」。

### Q: 数据库操作提示权限不足？
A: 检查集合权限设置，确保是「所有用户可读，仅创建者可写」。

### Q: 管理员看不到管理后台？
A: 确认 `users` 集合中 `isAdmin` 字段是布尔值 `true`（不是字符串），用户需要重新进入小程序。

### Q: 订阅消息收不到？
A: 
- 确认模板ID配置正确
- 确认用户在订餐时授权了订阅消息
- 测试期间只有开发者和体验者能收到
- 一次授权只能发送一条消息

## 后续扩展建议

1. 添加营养成分展示
2. 实现菜品分类和搜索
3. 添加批量订餐功能
4. 实现在线支付
5. 添加食堂公告功能
6. 实现菜品图片上传
7. 添加导出订单功能
8. 实现更多维度的数据分析

## 技术支持

如有问题，请检查：
1. 微信开发者工具控制台错误信息
2. 云开发控制台云函数日志
3. 数据库集合权限设置

---

**文档版本**：v1.0  
**最后更新**：2026-04-02
