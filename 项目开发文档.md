# 食堂订餐小程序 - 项目开发文档

---

## 一、项目概述

### 1.1 项目简介

基于微信小程序的食堂订餐系统，支持菜单管理、在线订餐、订单管理、数据统计、用户身份验证等功能。

**核心特性**：
- 统一订餐页面：整合首页和订餐页为一体
- 身份验证：用户需验证身份后才能使用
- 多角色支持：普通工作人员、管理员、厨房工作人员
- 菜品统计：订餐名单显示每道菜需要备多少份
- 手机号标识：用户识别使用 phone 字段

### 1.2 技术栈

| 类别 | 技术 |
|------|------|
| 前端 | 微信小程序原生开发 (WXML + WXSS + JS) |
| 后端 | 微信云开发 (云函数、云数据库) |
| 订阅消息 | 微信订阅消息 API |
| 身份验证 | 微信手机号授权 + 单位人员列表 |

### 1.3 项目结构

```
miniprogram-1/
├── miniprogram/                 # 小程序前端代码
│   ├── app.js                  # 应用入口（初始化、云函数调用）
│   ├── app.json                # 应用配置
│   ├── app.wxss                # 全局样式
│   ├── config.js               # 配置文件（订阅消息模板ID）
│   ├── utils/                  # 工具类目录
│   │   ├── auth.js             # 权限检查工具类
│   │   ├── dateUtil.js         # 日期工具类
│   │   └── initUtil.js         # 异步初始化工具类
│   └── pages/                  # 页面目录
│       ├── auth/               # 身份验证页
│       ├── index/              # 首页（保留但已不使用）
│       ├── order/              # 订餐页（统一页面）
│       ├── history/            # 历史订单
│       ├── profile/            # 个人中心
│       ├── admin/              # 管理后台入口
│       ├── menu/               # 菜单管理
│       ├── statistics/         # 数据统计
│       ├── evaluate/           # 评价页面
│       ├── orderList/          # 订餐名单
│       ├── settings/           # 订餐设置
│       ├── userManage/         # 用户管理
│       ├── reminderSettings/   # 提醒设置
│       ├── orderStats/         # 订餐统计
│       └── help/               # 使用帮助
├── cloudfunctions/              # 云函数目录
│   ├── quickstartFunctions/     # 通用云函数
│   ├── orderFunctions/          # 订单相关云函数
│   └── sendSubscribeMessage/   # 订阅消息发送云函数
└── 项目开发文档.md              # 本文档
```

---

## 二、数据库设计

### 2.1 集合列表

| 集合名 | 说明 | 权限建议 |
|--------|------|----------|
| users | 用户集合（已验证用户） | 创建者可读写 |
| staffs | 单位人员列表 | 云函数全权处理写入 |
| menus | 菜单集合 | 管理端可读写 |
| orders | 订单集合 | 创建者可读写 |
| evaluations | 评价集合 | 创建者可读写 |
| configs | 配置集合 | 仅管理端可写 |

### 2.2 users（用户集合 - 已验证用户）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  phone: "13800138000",           // 手机号（唯一标识）
  name: "张三",                    // 员工姓名
  isVerified: true,               // 是否已通过身份校验
  role: "staff",                  // 角色：staff/admin/kitchen
  userInfo: {},                   // 微信用户信息
  subscribeOrderReminder: true,   // 订餐提醒开关
  subscribeMealReminder: true,    // 用餐提醒开关
  createTime: Date,
  updateTime: Date
}
```

### 2.3 staffs（单位人员列表）

```javascript
{
  _id: "自动生成",
  phone: "13800138000",         // 手机号（唯一标识）
  name: "张三",                  // 姓名
  role: "staff",                // 角色：staff/admin/kitchen
  status: "active",             // 状态：active/inactive
  createTime: Date,
  updateTime: Date
}
```

### 2.4 menus（菜单集合）

```javascript
{
  _id: "自动生成",
  date: "2026-04-03",           // 日期，格式 YYYY-MM-DD
  meals: [                       // 餐次列表
    {
      type: "早餐",              // 餐次类型：早餐/午餐/晚餐
      dishes: [                  // 菜品列表
        { name: "豆浆" },
        { name: "油条" }
      ]
    },
    {
      type: "午餐",
      dishes: [
        { name: "红烧肉" },
        { name: "青菜" }
      ]
    }
  ],
  createTime: Date,
  updateTime: Date
}
```

### 2.5 orders（订单集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",        // 用户唯一标识
  phone: "13800138000",          // 手机号（用于用户识别）
  date: "2026-04-03",           // 订餐日期
  mealType: "早餐",              // 餐次类型：早餐/午餐/晚餐
  dishes: [                      // 菜品列表
    { name: "豆浆" },
    { name: "油条" }
  ],
  status: "pending",              // 订单状态
  evaluated: false,               // 是否已评价
  createTime: Date,
  updateTime: Date
}
```

**status 状态说明**：
- `pending`：待用餐（默认）
- `completed`：已完成（自动计算，详见订单状态逻辑）
- `cancelled`：已取消

### 2.6 evaluations（评价集合）

```javascript
{
  _id: "自动生成",
  _openid: "用户的openid",
  orderId: "订单id",
  dishName: "豆浆",              // 菜品名称
  mealType: "早餐",             // 餐次类型
  rating: 5,                    // 评分 1-5
  comment: "很好吃",            // 评价内容
  createTime: Date
}
```

### 2.7 configs（配置集合）

```javascript
{
  _id: "自动生成",
  key: "order_deadline",        // 配置键名
  breakfast_deadline: "08:00",   // 早餐截止时间
  lunch_deadline: "12:00",      // 午餐截止时间
  dinner_deadline: "17:00",     // 晚餐截止时间
  breakfast_meal_start: "08:00", // 早餐开饭时间
  lunch_meal_start: "12:00",     // 午餐开饭时间
  dinner_meal_start: "17:30",   // 晚餐开饭时间
  updateTime: Date
}
```

---

## 三、云函数

### 3.1 quickstartFunctions（通用云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| getOpenId | 获取用户openid | - | openid, appid, unionid |
| getPhoneNumber | 获取手机号 | code | phoneNumber |
| getStaffList | 获取员工列表 | - | 员工列表 |
| addStaff | 添加员工 | data | success/error |
| updateStaff | 更新员工 | id, data | success/error |
| deleteStaff | 删除员工 | id | success/error |
| createCanteenCollections | 创建数据库集合 | - | success |
| updateDeadlineConfig | 更新截止时间配置 | data | success/error |
| getDeadlineConfig | 获取截止时间配置 | - | deadline配置 |
| cancelOrder | 取消订单 | orderId, phone | success/error |
| getOrderStats | 获取订餐统计 | phone | thirtyDay, total |

### 3.2 orderFunctions（订单相关云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| getMenu | 获取菜单 | date | 菜单数据 |
| submitOrder | 提交订单 | date, mealType, dishes, phone, existingOrderId | success, orderId |
| getOrderList | 获取订单列表 | date | 订单列表 |
| getStatistics | 获取统计数据 | date | totalOrders, dishStats |
| submitEvaluation | 提交评价 | orderId, evaluations | success |

### 3.3 sendSubscribeMessage（订阅消息云函数）

| 方法名 | 功能 | 入参 | 返回 |
|--------|------|------|------|
| - | 发送订阅消息 | templateId, data, page | success/error |

---

## 四、核心功能逻辑

### 4.1 日期工具类 (dateUtil.js)

#### 功能说明

统一的日期处理工具，提供日期格式化、中文显示、前一天/后一天计算等功能。

#### 核心方法

| 方法 | 说明 |
|------|------|
| `formatDate(date)` | 格式化日期为 YYYY-MM-DD |
| `formatDateChinese(date)` | 格式化日期为中文（今天/明天/昨天 + 月日） |
| `formatDateChineseShort(date)` | 短格式中文日期（月日（今天）） |
| `isPast(dateStr)` | 判断是否是过去的日期 |
| `prevDay(dateStr)` | 计算前一天 |
| `nextDay(dateStr)` | 计算后一天 |

### 4.2 权限检查工具类 (auth.js)

#### 核心方法

| 方法 | 说明 |
|------|------|
| `checkVerified()` | 检查是否已通过身份校验 |
| `getRole()` | 获取当前用户角色 |
| `canAccess(pageType)` | 检查是否有权限访问指定页面 |
| `checkPageAccess(pageType)` | 页面权限检查，自动跳转 |
| `isInitializing()` | 检查是否正在初始化中 |

#### 角色权限定义

| 角色 | tabBar 页面 | 菜单管理 | 订餐名单 | 数据统计 | 订餐设置 | 用户管理 |
|------|------------|---------|---------|---------|---------|---------|
| staff | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| kitchen | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

### 4.3 异步初始化工具类 (initUtil.js)

#### 核心方法

| 方法 | 说明 |
|------|------|
| `waitForAppInit()` | 返回 Promise，等待 `openid` **和** `isVerified` 都存在 |
| `waitForOpenid()` | 返回 Promise，仅等待 `openid` |
| `isInitializing()` | 检查是否正在初始化中 |

### 4.4 身份验证流程 (auth.js)

```
用户打开小程序
    ↓
app.js onLaunch() 获取 openid
    ↓
检查 isVerified 状态
    ↓
    ├─ 已验证 → 进入订餐页
    │
    └─ 未验证 → 跳转至 /pages/auth/auth
              ↓
         显示身份验证页面
              ↓
         用户输入/获取手机号
              ↓
         查询 staffs 集合验证身份
              ↓
         验证成功 → 更新 users 集合（含name字段）→ 进入订餐页
              ↓
         验证失败 → 提示错误信息
```

### 4.5 订餐页面 (order)

#### 核心功能

- 整合原首页和订餐页功能
- 浏览每日菜单
- 选择菜品
- 提交/修改订单
- 截止时间控制

#### 核心数据结构

```javascript
data: {
  selectedDate: '2026-04-03',        // 当前选择的日期
  selectedDateStr: '今天 4月3日',     // 中文显示
  menuData: { ... },                 // 菜单数据
  orders: {
    '早餐': { orderId: null, dishes: [], status: 'none' },
    '午餐': { orderId: null, dishes: [], status: 'none' },
    '晚餐': { orderId: null, dishes: [], status: 'none' }
  },
  userSelections: {
    '早餐': [],
    '午餐': [],
    '晚餐': []
  },
  deadlines: {
    breakfast: '08:00',
    lunch: '12:00',
    dinner: '17:00'
  },
  disabledMeals: {
    '早餐': false,
    '午餐': false,
    '晚餐': false
  }
}
```

### 4.6 订单状态逻辑

#### 取消预约判断 (canCancel)

根据**截止时间**判断是否可取消：

| 时间段 | 订单状态 | 取消按钮 | 无法取消文案 | 评价按钮 |
|--------|---------|---------|------------|---------|
| 未过截止时间 | 待用餐 | ✅ 显示 | ❌ | ❌ |
| 过了截止，未到开饭 | 待用餐 | ❌ 隐藏 | ✅ 显示 | ❌ |
| 过了开饭时间 | 已完成 | ❌ 隐藏 | ❌ | ✅ 显示 |

#### 取消订单云函数 (cancelOrder)

通过云函数取消订单，包含截止时间校验：

```javascript
const cancelOrder = async (event, openid) => {
  const { orderId, phone } = event;

  // 1. 验证订单归属（使用 phone 验证）
  if (order.phone !== phone) {
    return { success: false, error: '无权操作此订单' };
  }

  // 2. 检查状态
  if (order.status === 'cancelled') {
    return { success: false, error: '该订单已取消' };
  }

  // 3. 截止时间校验
  // ...

  // 4. 执行取消
  await db.collection('orders').doc(orderId).update({
    data: { status: 'cancelled', updateTime: new Date() }
  });
  return { success: true };
};
```

### 4.7 订餐名单页面 (orderList)

#### 核心功能

- **外层显示**：每顿饭的订单数和每道菜需要备的份数
- **点击跳转**：点击餐次卡片可查看订单明细
- **明细页面**：只显示菜品列表，不显示用户信息

### 4.8 个人中心 (profile)

#### 核心功能

- **用户卡片**：展示头像、员工姓名、加密手机号、角色标签
- **提醒设置**：跳转至提醒设置页面
- **订餐统计**：跳转至订餐统计页面
- **使用帮助**：跳转至帮助页面
- **管理后台**：admin/kitchen 角色可见

### 4.9 订阅消息

#### 配置

在 `miniprogram/config.js` 中配置：

```javascript
module.exports = {
  subscribeMessageTemplateId: 'tZAu7qUAcWS1T6d99m7elTaOkFZpi2VudxQu5pqxM70',
  getSubscribeMessageTemplateId() {
    return this.subscribeMessageTemplateId;
  }
}
```

#### 发送流程

```
用户点击"订餐"
    ↓
requestSubscribeMessage() - 请求用户授权
    ↓
用户同意/拒绝
    ↓
doSubmitOrder() - 提交订单
    ↓
sendSubscribeMessage() - 调用云函数发送消息
```

#### 用户偏好设置

用户可在「个人中心 → 提醒设置」中开启/关闭两类提醒。设置保存在 `users` 集合的 `subscribeOrderReminder` 和 `subscribeMealReminder` 字段中。

---

## 五、页面路由

### 5.1 tabBar 页面（底部导航）

| 页面 | 路径 | 说明 |
|------|------|------|
| 订餐 | /pages/order/order | 统一订餐页面 |
| 订单 | /pages/history/history | 历史订单 |
| 我的 | /pages/profile/profile | 个人中心 |

### 5.2 身份验证页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 身份验证 | /pages/auth/auth | 首次使用或未验证用户 |

### 5.3 管理端页面

| 页面 | 路径 | 说明 | 权限 |
|------|------|------|------|
| 管理后台 | /pages/admin/admin | 入口页面 | admin/kitchen |
| 菜单管理 | /pages/menu/menu | 设置每日菜单 | admin/kitchen |
| 订餐名单 | /pages/orderList/orderList | 查看订餐用户 | admin/kitchen |
| 数据统计 | /pages/statistics/statistics | 订餐数据 | admin/kitchen |
| 订餐设置 | /pages/settings/settings | 截止时间配置 | admin/kitchen |
| 用户管理 | /pages/userManage/userManage | 单位人员管理 | admin |

### 5.4 功能页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 评价 | /pages/evaluate/evaluate | 评价菜品 |
| 提醒设置 | /pages/reminderSettings/reminderSettings | 提醒开关 |
| 订餐统计 | /pages/orderStats/orderStats | 个人订餐统计 |
| 使用帮助 | /pages/help/help | 功能说明 |

---

## 六、初始化与部署

### 6.1 初始化数据库

1. 上传并部署 `cloudfunctions/quickstartFunctions` 云函数
2. 上传并部署 `cloudfunctions/orderFunctions` 云函数
3. 上传并部署 `cloudfunctions/sendSubscribeMessage` 云函数
4. 在小程序首页点击"初始化数据库"按钮
5. 云函数会自动创建集合并插入默认配置

### 6.2 设置管理员

1. 在云开发控制台找到 `staffs` 集合
2. 添加一条新记录：

```javascript
{
  phone: "您的手机号",
  name: "管理员",
  role: "admin",
  status: "active"
}
```

3. 使用该手机号登录小程序完成身份验证

### 6.3 配置订阅消息

1. 在微信公众平台申请订阅消息模板
2. 将模板ID填入 `miniprogram/config.js`

---

## 七、注意事项

### 7.1 手机号标识

- 订单查询使用 `phone` 字段作为用户标识
- 前端从 `app.globalData.phone` 获取
- 取消订单云函数使用 `phone` 验证订单归属

### 7.2 openid 使用

- openid 仍记录在订单中用于微信消息发送
- 前端直接操作数据库时，不能使用 `'{openid}'` 占位符
- 使用 `initUtil.waitForAppInit()` 确保 openid 已获取

### 7.3 日期格式化

- 数据库存储格式：`YYYY-MM-DD`
- 页面显示格式：`今天 4月3日`、`明天 4月4日`、`4月5日`
- 所有日期逻辑使用 `dateUtil.js` 工具类

### 7.4 订单设计

- 订单以**顿**为单位，不是以天为单位
- 每顿饭独立一条订单记录
- 取消预约只改变 status，不删除记录
- 订单按 phone 过滤，每个用户只能看到自己的订单

### 7.5 状态自动计算

- 订单状态由前端根据时间自动计算显示
- 数据库中 status 字段只有 `pending` 和 `cancelled` 两种变化
- `completed` 状态是前端计算出来的，不会写入数据库

---

## 八、数据操作规范

### 8.1 云函数封装

为保证数据安全和业务逻辑统一，建议将所有数据操作收敛到云函数中。

#### 可保持前端直查的操作

| 集合 | 操作 | 原因 |
|------|------|------|
| menus | 读取菜单 | 菜单是公开数据 |
| configs | 读取配置 | 配置是公开信息 |
| evaluations | 提交评价 | 评价功能保持简单 |

#### 需使用云函数操作

| 集合 | 操作 | 原因 |
|------|------|------|
| orders | 查询/提交/取消 | 需要 phone 过滤 + 归属验证 |
| users | 查询/更新用户信息 | 需要身份验证 |
| menus | 保存菜单 | 需要 admin/kitchen 权限 |
| configs | 保存配置 | 需要 admin/kitchen 权限 |

详见：`数据操作收敛方案.md`

---

## 九、版本更新记录

### v1.3 版本

#### 功能更新

- **用户卡片**：个人中心展示头像、姓名、加密手机号、角色标签
- **提醒设置**：新增 reminderSettings 页面，支持订餐/用餐提醒开关
- **订餐统计**：新增 orderStats 页面，展示近30天和总计订餐次数
- **使用帮助**：新增 help 页面，按角色展示功能说明
- **手机号标识**：订单查询从 _openid 改为 phone

#### 数据库更新

- `orders` 集合新增 `phone` 字段
- `users` 集合新增 `name` 字段
- `users` 集合新增 `subscribeOrderReminder`、`subscribeMealReminder` 字段

#### 云函数更新

- `cancelOrder` 支持 `phone` 参数验证
- 新增 `getOrderStats` 云函数

### v1.2 版本

- 新增身份验证功能
- 新增用户管理功能
- 新增多角色支持
- 合并首页和订餐页为统一订餐页面

### v1.1 版本

- 订单以顿为单位
- 支持取消预约
- 新增日期工具类

### v1.0 版本

- 基础功能实现
