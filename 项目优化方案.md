# 食堂订餐小程序优化方案

## 一、项目现状分析

### 1.1 代码结构现状

**云函数**：
- `quickstartFunctions`：功能过于集中，包含用户验证、员工管理、订单管理、配置管理等多个模块，代码量超过600行
- `orderFunctions`：负责订单相关操作，功能相对集中
- `sendSubscribeMessage`：负责订阅消息发送

**前端代码**：
- 工具类已初步封装（auth.js、dateUtil.js、initUtil.js）
- 页面代码存在重复逻辑，如日期处理、权限检查等
- 缺少组件化开发，部分页面布局和功能重复

### 1.2 性能现状

**数据库查询**：
- 部分查询可能存在性能问题，如未使用索引
- 多次查询可能导致网络往返时间增加

**前端加载**：
- 页面加载时可能存在多次网络请求
- 缺少缓存机制，重复请求相同数据

## 二、代码结构优化方案

### 2.1 云函数拆分方案

#### 2.1.1 拆分策略

| 原云函数 | 拆分后云函数 | 职责 | 文件位置 |
|---------|------------|------|---------|
| quickstartFunctions | userFunctions | 用户验证、员工管理 | cloudfunctions/userFunctions/ |
| quickstartFunctions | configFunctions | 配置管理 | cloudfunctions/configFunctions/ |
| quickstartFunctions | menuFunctions | 菜单管理 | cloudfunctions/menuFunctions/ |
| orderFunctions | orderFunctions | 订单管理（保持不变） | cloudfunctions/orderFunctions/ |
| sendSubscribeMessage | sendSubscribeMessage | 订阅消息发送（保持不变） | cloudfunctions/sendSubscribeMessage/ |

#### 2.1.2 具体拆分实现

**1. 创建 userFunctions 云函数**
- 包含功能：用户验证、员工管理
- 文件：`cloudfunctions/userFunctions/index.js`
- 从 quickstartFunctions 迁移的方法：
  - verifyAndBindPhone
  - getOpenId
  - getPhoneNumber
  - checkIsAdmin
  - getStaffList
  - addStaff
  - updateStaff
  - deleteStaff
  - getMiniProgramCode

**2. 创建 configFunctions 云函数**
- 包含功能：配置管理
- 文件：`cloudfunctions/configFunctions/index.js`
- 从 quickstartFunctions 迁移的方法：
  - updateDeadlineConfig
  - getDeadlineConfig

**3. 创建 menuFunctions 云函数**
- 包含功能：菜单管理
- 文件：`cloudfunctions/menuFunctions/index.js`
- 从 quickstartFunctions 迁移的方法：
  - saveMenu
  - createCanteenCollections

**4. 调整 orderFunctions 云函数**
- 保持现有功能不变
- 从 quickstartFunctions 迁移的方法：
  - cancelOrder
  - getOrderStats

#### 2.1.3 云函数配置更新

**1. 为新创建的云函数添加 package.json 文件**
```json
{
  "name": "userFunctions",
  "version": "1.0.0",
  "description": "用户相关云函数",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "^2.8.0"
  }
}
```

**2. 更新 cloudbaserc.json 配置**
```json
{
  "version": "2.0",
  "envId": "cloudbase-9gsdyt4b14de5384",
  "functionRoot": "./cloudfunctions",
  "functions": [
    {
      "name": "userFunctions",
      "config": {
        "timeout": 60,
        "memorySize": 256,
        "runtime": "Nodejs10.15"
      }
    },
    {
      "name": "configFunctions",
      "config": {
        "timeout": 60,
        "memorySize": 256,
        "runtime": "Nodejs10.15"
      }
    },
    {
      "name": "menuFunctions",
      "config": {
        "timeout": 60,
        "memorySize": 256,
        "runtime": "Nodejs10.15"
      }
    },
    {
      "name": "orderFunctions",
      "config": {
        "timeout": 60,
        "memorySize": 256,
        "runtime": "Nodejs10.15"
      }
    },
    {
      "name": "sendSubscribeMessage",
      "config": {
        "timeout": 60,
        "memorySize": 256,
        "runtime": "Nodejs10.15"
      }
    }
  ]
}
```

### 2.2 前端代码模块化方案

#### 2.2.1 工具类扩展

**1. 增强 dateUtil.js**
- 添加更多日期处理方法，如日期范围计算、工作日判断等
- 优化现有方法，提高性能

**2. 增强 auth.js**
- 封装更多权限检查方法
- 添加角色判断的辅助函数

**3. 新增 httpUtil.js**
- 封装云函数调用，统一错误处理
- 实现请求缓存机制

```javascript
// utils/httpUtil.js
export const callCloudFunction = async (name, data, options = {}) => {
  try {
    const result = await wx.cloud.callFunction({
      name,
      data,
      ...options
    });
    return result.result;
  } catch (error) {
    console.error(`调用云函数 ${name} 失败:`, error);
    throw error;
  }
};

// 带缓存的云函数调用
export const callCloudFunctionWithCache = async (name, data, cacheKey, cacheTime = 5 * 60 * 1000) => {
  // 尝试从缓存获取
  const cachedData = wx.getStorageSync(cacheKey);
  const now = Date.now();
  
  if (cachedData && (now - cachedData.timestamp) < cacheTime) {
    return cachedData.data;
  }
  
  // 调用云函数
  const result = await callCloudFunction(name, data);
  
  // 缓存结果
  wx.setStorageSync(cacheKey, {
    data: result,
    timestamp: now
  });
  
  return result;
};
```

#### 2.2.2 组件化开发

**1. 创建日期选择组件**
- 文件：`components/datePicker/index.js`
- 功能：统一的日期选择器，支持前后切换、日期显示

**2. 创建餐次选择组件**
- 文件：`components/mealSelector/index.js`
- 功能：餐次选择和展示

**3. 创建订单状态组件**
- 文件：`components/orderStatus/index.js`
- 功能：根据订单状态显示不同样式和操作按钮

**4. 创建评价星级组件**
- 文件：`components/ratingStar/index.js`
- 功能：星级评分展示和选择

#### 2.2.3 页面代码优化

**1. 统一页面初始化逻辑**
- 提取页面通用初始化方法，如权限检查、数据加载等
- 减少重复代码

**2. 优化页面数据处理**
- 使用计算属性和数据缓存
- 减少不必要的setData调用

## 三、性能优化方案

### 3.1 数据库查询优化

#### 3.1.1 索引优化

**为以下字段添加索引**：

| 集合 | 字段 | 索引类型 | 用途 |
|------|------|----------|------|
| users | phone | 唯一索引 | 快速查找用户 |
| users | _openid | 唯一索引 | 快速查找用户 |
| orders | phone | 普通索引 | 按用户查询订单 |
| orders | date | 普通索引 | 按日期查询订单 |
| orders | status | 普通索引 | 按状态查询订单 |
| menus | date | 唯一索引 | 快速查找菜单 |
| staffs | phone | 唯一索引 | 快速验证员工 |

**创建索引的方法**：
1. 在云开发控制台中为对应集合创建索引
2. 或使用云函数创建索引：

```javascript
// 创建索引示例
const createIndexes = async () => {
  await db.collection('orders').createIndex({
    phone: 1
  });
  await db.collection('orders').createIndex({
    date: 1
  });
  // 其他索引...
};
```

#### 3.1.2 查询优化

**1. 使用 db.command 进行高效查询**
```javascript
// 优化前
const orders = await db.collection('orders').where({
  date: event.date,
  status: 'pending'
}).get();

// 优化后
const orders = await db.collection('orders').where({
  date: event.date,
  status: _.neq('cancelled')
}).get();
```

**2. 合理使用 Promise.all 并行处理**
```javascript
// 优化前
const menu = await getMenu(date);
const orders = await getOrders(date);
const config = await getConfig();

// 优化后
const [menu, orders, config] = await Promise.all([
  getMenu(date),
  getOrders(date),
  getConfig()
]);
```

**3. 限制返回字段**
```javascript
// 优化前
const users = await db.collection('users').get();

// 优化后
const users = await db.collection('users').field({
  phone: true,
  name: true,
  role: true
}).get();
```

### 3.2 前端加载优化

#### 3.2.1 网络请求优化

**1. 合并请求**
- 将多个相关请求合并为一个云函数调用
- 减少网络往返次数

**2. 使用缓存**
- 缓存配置、菜单等不频繁变化的数据
- 减少重复请求

**3. 延迟加载**
- 非关键数据延迟加载
- 提高页面初始加载速度

#### 3.2.2 渲染优化

**1. 减少 setData 调用**
- 合并多次 setData 为一次
- 避免在循环中使用 setData

**2. 使用 virtual-list**
- 对于长列表，使用虚拟列表技术
- 只渲染可视区域内的元素

**3. 优化图片加载**
- 使用图片懒加载
- 压缩图片大小

## 四、实施步骤

### 4.1 云函数拆分实施

1. **创建新的云函数目录**：
   - `cloudfunctions/userFunctions/`
   - `cloudfunctions/configFunctions/`
   - `cloudfunctions/menuFunctions/`

2. **迁移代码**：
   - 将 quickstartFunctions 中的相关代码迁移到新的云函数
   - 调整函数调用方式

3. **更新配置**：
   - 更新 cloudbaserc.json
   - 为新云函数添加 package.json

4. **部署云函数**：
   - 部署新创建的云函数
   - 验证云函数调用正常

### 4.2 前端代码优化实施

1. **创建组件**：
   - 实现日期选择、餐次选择等组件
   - 集成到现有页面

2. **扩展工具类**：
   - 实现 httpUtil.js
   - 增强现有工具类

3. **优化页面代码**：
   - 应用组件化开发
   - 优化数据处理逻辑

### 4.3 性能优化实施

1. **创建数据库索引**：
   - 在云开发控制台创建索引
   - 或使用云函数创建

2. **优化查询代码**：
   - 应用查询优化技巧
   - 测试查询性能

3. **前端加载优化**：
   - 实现缓存机制
   - 优化渲染逻辑

## 五、预期效果

### 5.1 代码结构优化效果

- **云函数职责清晰**：每个云函数只负责特定领域的功能
- **代码可维护性提高**：减少单个文件的代码量，便于理解和维护
- **开发效率提升**：团队成员可以并行开发不同功能模块
- **部署速度加快**：云函数体积减小，部署时间缩短

### 5.2 性能优化效果

- **数据库查询速度提升**：索引优化和查询优化减少查询时间
- **前端加载速度提升**：缓存机制和请求合并减少网络请求时间
- **用户体验改善**：页面加载更快，操作更流畅
- **系统稳定性提高**：减少不必要的网络请求和数据库操作

## 六、注意事项

1. **兼容性**：确保云函数拆分后，前端调用方式的兼容性
2. **测试**：在实施优化后，进行充分的测试，确保功能正常
3. **监控**：部署后监控系统性能，及时发现和解决问题
4. **文档**：更新项目文档，记录优化后的架构和使用方法

## 七、总结

通过本次优化，项目将获得以下收益：

1. **代码结构更加清晰**：云函数按功能拆分，前端代码组件化
2. **性能显著提升**：数据库查询优化和前端加载优化
3. **可维护性增强**：代码结构合理，易于理解和维护
4. **扩展性更好**：模块化设计便于后续功能扩展

优化工作是一个持续的过程，建议定期评估系统性能，不断进行优化和改进。